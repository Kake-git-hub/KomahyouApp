# 仕様: 「要望・報告」ボタン（利用者からの不具合報告・追加要望。旧称「開発者へ報告」）

- 正本: この文書。実装は `src/utils/operationTrace.ts` / `src/utils/developerReport.ts` /
  `src/components/developer-report/DeveloperReportModal.tsx` / `functions/src/developerReport.ts`(＋`index.ts` の
  `submitDeveloperReport`) / `tools/developer-report-notify.mjs` / `.github/workflows/developer-reports.yml`。
- 決定日: 2026-09-04（オーナー指示）。背景: 室長から「この一ヶ月でバグがあったが忙しくて自分で対処した」と後から
  連絡があり、何が起きたか追えなかった。**忙しくてもボタン1つ・数秒で開発者へ知らせられる**導線を用意する。

## A. 目的と非目的

- 目的: 利用者が「おかしいな」と感じた瞬間に、**直近の操作履歴と画面のデータ**を開発者へ届け、
  バグ／仕様の誤解／勘違いを開発者側で切り分けられるようにする。
- **受け取った後の進め方（オーナー指示 2026-09-04・厳守）**: 利用者報告 Issue を見ても**勝手に修正を始めない**。
  調査・整理までは進めてよいが、コード修正への着手はオーナーが確認して許可してから。
- 非目的: 全操作をサーバーへ常時記録すること（コストとノイズが見合わない。オーナー判断 2026-09-04）。
  本番での不変条件(INV)自己検査（オーナー判断で不要。報告が来たら開発者が検査する）。

## B. ボタンの位置と見え方（オーナー確定）

- ボタン名は **「要望・報告」**（オーナー確定 2026-09-04。不具合だけでなく追加要望も同じ導線で送る）。
- **コマ表(盤面)**: ツールバーの「講師日程共有」の**右**に「要望・報告」。同じ形(secondary/slim)で**色だけ変える**
  (淡いオレンジ・`report-developer-button`)。テンプレ編集モードでは出さない（講師日程共有と同じ）。
- **日程表(別タブ・生徒/講師)**: ツールバーの「登録された講習期間を表示する」の**右**に「要望・報告」。
  同じ枠線ボタンで色だけ変える(`button.report-developer`)。印刷用全員表示(all-view)には出さない（ツールバー自体が無い）。
- 日程表は**操作せず表示だけ見て**「おかしい」と思うことがある。そのため日程表側のボタンは、その日程表で
  表示していた条件（種別・期間・講習期間ラベル・選択人物・検索語）を報告に添える。
- **モーダルは盤面・日程表で同一の表示・文言**（オーナー指示 2026-09-04）。文言の正本は
  `DEVELOPER_REPORT_UI_TEXT`（`src/utils/developerReport.ts`）で、日程表 HTML の埋め込みスクリプトへ生成時に埋め込む。
  大きく読みやすく（幅 820px・本文 17px・入力欄 7 行）。一言が空なら送信せず「一言を入力してください」と促す。

## C. 送信内容（`submitDeveloperReport`）

| 項目 | 内容 | 出どころ |
|---|---|---|
| 種類 | 不具合・おかしい(bug) ／ 追加してほしい・要望(request) ／ 使い方の質問(question・2026-09-13 実装。§G)。既定は bug | 同一モーダルのラジオ |
| 内容 | **必須**（2026-09-04 改定・空欄は送れない。上限 2000 字） | 盤面・日程表とも同一モーダルの textarea |
| テスト扱い | 内容に `#テスト`（または `#test`）を含むと **Issue を起票しない**（`notifiedAt` を即時に埋める）。メールは【テスト】付きで届く | サーバーが判定 |
| 直近の操作痕跡 | 端末内リングバッファの最新 300 件 | `operationTrace.ts`（§D） |
| 報告時点の教室データ | メモリ上の教室データ（**未保存の変更込み**）を gzip して Storage へ | `buildClassroomSnapshotPayload` |
| メタ | 教室・報告元(board/schedule)・アプリ版数・UA・URL・画面・未保存有無・最終保存時刻 | App |
| 実行者・受領時刻 | **サーバーが付ける**（自己申告にしない） | Cloud Function |

- 保存先: Firestore `workspaces/{ws}/developerReports/{reportId}`（メタ＋操作痕跡）、
  Storage `developer-reports/{ws}/{classroomId}/{reportId}.json.gz`（教室データ）。
- **本番データ（classroomSnapshots 等）には一切書かない。** 権限は保存と同じ `requireClassroomAccessMember`。
- 教室データの Storage 保存に失敗しても報告本体は残す（`snapshotStoragePath` 空・`snapshotByteLength=-1`）。

## D. 操作痕跡（端末内・全操作）

- 対象: 盤面が変わる全確定（`commitWeeks` の前後差分＝「日付 限 机番: 前 → 後」）、テンプレ反映(board-rebuild)、
  undo/redo、保存の成否、操作ログ(`operationLog.ts`)のイベントの写し、日程表など別タブからの postMessage、
  報告モーダルの開閉・送信結果。
- 保持: 教室ごとに最新 300 件をメモリ＋`localStorage`(`operation-trace:<classroomId>`)。再読み込みを跨いで残る。
- **報告ボタンが押されるまで端末外へ出ない。** 記録の失敗は本体の操作に影響させない（例外は握る）。
- 操作ログ（サーバー監査記録・在庫が減る/記録が消える操作だけ）とは別物。両者の種別一覧はサーバー
  `DEVELOPER_REPORT_TRACE_KINDS` と二重管理（テストで一致を固定）。

## E. 開発者が受け取る仕組み

- **メール即時通知（オーナー要望 2026-09-04・LINE の代替）**: Cloud Function `notifyDeveloperReportByMail` が
  `developerReports` 文書の作成をトリガに SMTP で送る。私的経路なので操作痕跡（直近 60 件・生徒名を含む）も本文に載せる。
  設定は functions runtime env（ローカル `functions/.env`・CI は repo secret `PROD_FUNCTIONS_ENV`）の
  `REPORT_MAIL_SMTP_URL`（例: `smtps://you%40gmail.com:アプリパスワード@smtp.gmail.com:465`）と `REPORT_MAIL_TO`
  （任意 `REPORT_MAIL_FROM`）。未設定なら送らず `mailSkipped` を記録。成否は文書の `mailSentAt` / `mailError`。
  手順は `docs/runbooks/monitoring.md`。**GitHub の通知設定は不要**（Issue はあくまで課題管理の記録）。

- `.github/workflows/developer-reports.yml` が **15 分ごと**に `notifiedAt == null` の報告を Firestore REST で拾い、
  GitHub Issue を起票（ラベル `type:bug` / `status:triage` / `source:user-report`）→ `notifiedAt`・`issueNumber` を埋める。
  開発者は GitHub 通知（メール/アプリ）で受け取る。認証は既存 secret `RE_FIREBASE_SERVICE_ACCOUNT`。
- **公開リポジトリのため Issue 本文に操作痕跡（生徒名）・教室データは載せない。** メタ＋一言＋置き場所
  （Firestore 文書パス／Storage パスと取得コマンド）だけ。Issue 本文にも「勝手に修正を始めない・許可後に着手」を明記。
- LINE 通知は **不採用**（オーナー判断 2026-09-04: Messaging API の取得条件が厳しくなったため。メール直送へ置換）。
- Issue のラベルは種類で変える: bug → `type:bug`、request → `type:feature`、question → `type:question`（共通 `status:triage` / `source:user-report`）。
  Issue タイトルの種別は `[利用者報告]` / `[利用者要望]` / `[利用者質問]`。メール件名は質問のとき `【質問】` を先頭に付ける（テスト送信は `【テスト】【質問】` の順）。
- Firestore ルール: `developerReports` は開発者のみ read、write は不可（Cloud Function は Admin SDK で書く）。
  ルールの反映は `firebase deploy --only firestore:rules`（main マージでは反映されない）。

## E-2. 開発用教室の確認リスト（2026-09-12 オーナー指示）

- 開発用教室（`isActingDevelopmentClassroom` が真の教室）でだけ、画面最前面に確認事項チェックリストの
  パネルを出す。**本番教室には一切出さない**（`src/App.tsx` で条件付けし、source-scan テスト
  `VerificationChecklistPanel.wiring.test.ts` で固定）。
- 項目の正本は `src/utils/verificationChecklist.ts`（`VERIFICATION_CHECKLIST`）。各項目は 未確認／OK／要改善 の
  3 択とメモを持ち、入力のたびに localStorage（`verification-checklist:<教室ID>:<版>`）へ下書き保存する。
- **「保存して送信」は同じ経路で送る**: 既存の `submitDeveloperReport`（`source: board` / `category: request`）を
  そのまま使い、developerReports に蓄積＋メール通知される。新しい保存先・別の送信口は作らない。
- 本文は `buildVerificationChecklistReportNotes` が整形する。先頭行は目印 `[確認リスト <版>]`、未確認の項目は
  省略、`- <id> OK` / `- <id> 要改善: <メモ>` の圧縮書式。`DEVELOPER_REPORT_NOTE_LIMIT`(2000字)を超える場合は
  複数通に分け、各通の先頭に同じ目印と `(i/n)` を付ける。**送信本文に生徒名は入れない**（項目 id とメモだけ）。
- 送信できないときの保険として「Markdown をコピー」も用意する。送信後も下書きは残る（再送できる）。
## F. 受け入れ条件（テストで固定）

- 一言が空なら盤面・日程表とも送れない（`validateDeveloperReportNote`。本体側でも日程表経由の空文を弾く）。
- 操作痕跡は上限件数・上限文字数で切り、教室未登録なら記録しない（別教室混入防止）。
- 盤面差分は変わった机だけを列挙し、机に現れない付随変更（休日・在庫・補正）も名前で残す。
- サーバーは壊れた要素だけ捨てて残りを通し、`classroomId` は権限確認済みの値を使う。
- Issue 本文に生徒名（操作痕跡）・メールアドレスが載らない。起票→記録の順で二重起票を防ぐ。
- 日程表 HTML にボタンと送受信の両メッセージ種別が含まれ、埋め込みスクリプトが構文的に妥当。
- 種類 `question`（§G・2026-09-13 実装）は client（`DEVELOPER_REPORT_CATEGORIES` / `categoryOptions`）・server（正規化・メール件名【質問】）・
  Issue（ラベル `type:question`・タイトル `[利用者質問]`）の全経路で認識され、client と server の種別一覧はテストで一致を固定する
  （`developerReport.test.ts` 両側 / `developer-report-notify.test.mjs` / `scheduleHtml.test.ts`）。質問を選んだときだけ
  「回答は開発者が確認してからお返しします（すぐには返りません）」の注意文が盤面・日程表の両モーダルに出る（§G-2）。

---

# 追記（2026-09-12）: 質問種別と「蓄積 → まとめ読み」

> 出どころ: `docs/plan-2026-09-11-five-requests.md` §3（第4版）。段階 **Q-1**（spec-curator）の成果物。
> §A〜§F（2026-09-04 確定）は**改変していない**。特に §E「勝手に修正を始めない（オーナー許可後に着手）」は
> 質問種別にもそのまま適用する。

## ＊ オーナー確認待ち（仮置き。確定までは「暫定」）

計画 §9-2 の未回答 4 点。以下は **spec-curator の推奨で仮置き**したもので、**確定はオーナーに帰属**する。
確定するまで実装（Q-2 以降）は進めてよいが、**利用者へ回答を返す運用の開始（Q-5 の「承認して返答」）は
② が確定してから**とする。

| # | 論点 | 推奨（仮置き） | 代替案 | 決めないと困る理由 |
|---|---|---|---|---|
| ① | 後で読む入口 | **ルート A（Claude Code スキル `report-digest`）＋ルート B（任意 AI に貼る）の 2 本立て** | A のみ／B のみ／将来 C（API）を先に作る | 入口が決まらないとダイジェストパックの出力形式（Markdown 1 本か機械可読 JSON か）が決められず、Q-3 の手戻りになる |
| ② | 「公開してよい回答」の基準 | **§G-4 の 7 項目（全部を満たすものだけ返答可）** | 基準を設けず 1 件ずつオーナーが判断／もっと緩い最小 3 項目 | 基準が無いと承認画面（Q-5）のチェック項目が作れず、「公開したくない回答が出る不安」（オーナー懸念）が解消しない |
| ③ | QA の公開範囲の既定 | **全教室共有。ただし公開は既定 OFF（1 件ごとに開発者が明示チェック）＋公開時に本文を編集可** | 送信元教室のみに公開／公開機能そのものを作らない（Q-8 まで保留） | 既定が「全教室」か「自教室のみ」かで `qaEntries` のデータ構造（classroomId の意味・ルール）が変わる。後から変えると移行が要る |
| ④ | API 自動処理（Q-7） | **将来オプションのまま据え置き（今回は作らない。入口の純関数だけ流用可能にする）** | 今回のうちに Cloud Functions 側の骨組みだけ作る | 据え置きが確定していれば `reportDigestPrompt.ts` を「貼り付け用の文字列生成」に振り切れる。将来やるなら最初から provider アダプタ形に作る必要がある |

## G. 質問（種別 `question`）

### G-1. 位置づけ（オーナー確定 2026-09-12・変更不可）

- 種別は **不具合(bug) ／ 要望(request) ／ 質問(question)** の 3 つ。**3 種別とも扱いは同じ**:
  **送信 → ストック（Firestore `developerReports`）→ メール通知 → 後で開発者が対応**。
  質問だけの特別な即時処理（自動応答・AI 即答）は**作らない**。
- **当面 API は使わない**（外部 AI への自動連携をアプリ／Cloud Functions から行わない）。
- **利用者への即答はしない。** 利用者が見られるのは**開発者が承認した回答だけ**（§G-3）。
- **AI には全データを渡す**（伏字化・除外はしない。§H-1）。**まとめ読みの実行頻度は決めない。**
- 非目的: 質問の自動分類・自動回答・類似 QA の即時提示（Q-8。§G-4 の基準が確定するまで保留）。

### G-2. 種別の追加位置（Q-2）

既存の導線・モーダル・通知はすべて**流用**する（新しい画面・新しいボタンを増やさない＝学習コストを上げない）。

| 触る場所 | 追加内容 |
|---|---|
| クライアント型 `src/utils/developerReport.ts` | `DEVELOPER_REPORT_CATEGORIES` に `'question'` を追加。既定は **`bug` のまま**（`normalizeDeveloperReportCategory` の既定値を変えない） |
| モーダル文言 `DEVELOPER_REPORT_UI_TEXT.categoryOptions` | 3 番目に `{ value: 'question', label: '使い方の質問' }`。**並び順は 不具合 → 要望 → 質問**（既存 2 つの順序と表示は変えない） |
| 日程表の埋め込みモーダル（`scheduleHtml.ts`） | 同じ文言正本から生成されるため、ラジオが 3 つになるだけ。**盤面と日程表で同一の表示・文言**（§B の原則を維持） |
| サーバー `functions/src/developerReport.ts` | 受け入れ種別に `question` を追加（二重管理はテストで一致を固定＝既存作法） |
| GitHub Issue ラベル | bug → `type:bug`／request → `type:feature`／**question → `type:question`**（共通 `status:triage` / `source:user-report` は据え置き） |
| メール件名 | 種別の接頭辞を付ける。質問は **【質問】**（テスト送信の【テスト】表記は既存どおり併記。実装: `【テスト】【質問】[コマ表アプリ 要望・報告] …` の順） |
| `#テスト` 扱い | 3 種別共通で既存どおり（Issue 起票なし・メールは届く） |

- 質問でも**操作痕跡・教室データのスナップショットは同じように添付**する（「どの画面を見て疑問に思ったか」が
  回答の前提になるため。除外しない）。
- 利用者への表示上の注意書きとして、質問を選んだときだけ**「回答は開発者が確認してからお返しします（すぐには返りません）」**を
  モーダルに出す（即答の期待を作らないため。文言正本は `DEVELOPER_REPORT_UI_TEXT`）。

### G-3. 回答フローと状態遷移（Q-5）

`developerReports/{reportId}` に回答用フィールドを足す（**既存フィールドは変更しない**）。

| 状態 | 条件（文書の状態） | 利用者から見えるか |
|---|---|---|
| **未回答** | `answerDraft` なし・`answeredAt` なし | 見えない |
| **回答案あり** | `answerDraft` あり・`answeredAt` なし | **見えない**（下書きは端末へ一切出さない） |
| **承認済み（返答済み）** | `answerFinal` ＋ `answeredAt` ＋ `answeredBy` あり | **見える**（§G-5 の通知経路のみ） |
| **QA 公開（任意）** | 上に加えて `qaEntries/{id}` が存在し `publishedAt` あり | 全教室（既定・仮置き ③）から閲覧可 |

- 遷移は**一方向**（未回答 → 回答案あり → 承認済み → 任意で QA 公開）。承認済みを未回答へ戻さない。
  内容を直すときは**承認済みのまま本文を更新**し（`answeredAt` は更新、`answerRevision` を +1）、
  返答通知を**再度未読にしない**（利用者を驚かせない）。
- 承認・公開の確定は **callable `answerDeveloperReport`（`requireDeveloperMember`）だけ**が書く。
  クライアントからの `developerReports` 直書きは従来どおり不可（§E のルール踏襲）。
- 承認画面（開発者画面のサブページ `'questions'`）は**一覧＋1 件表示**。1 件ごとに
  「AI 用プロンプトをコピー（1 件分のパック）」「回答案欄」「承認して返答」「QA として公開（既定 OFF）」を置く。
- **質問以外（bug / request）にも同じ回答欄を使ってよい**（「直しました」「要望として登録しました」を返せる）。
  ただし返答は任意で、既定は返さない（§E の「勝手に修正を始めない」と矛盾しないよう、**着手の約束をしない**文面にする）。

### G-4. 「公開してよい回答」の基準（叩き台・オーナー確定待ち＝仮置き ②）

利用者へ返す**回答本文の内容基準**。**AI へ渡す入力の制限ではない**（入力は §H-1 のとおり無制限）。
承認画面のチェックリストとして出し、**全項目を満たすものだけ「承認して返答」できる**。

1. **個別教室の中身に触れない**… 他教室はもちろん、当該教室の生徒名・講師名・具体的な配置内容を回答本文に書かない
   （「◯◯さんの 9/3 の振替は〜」ではなく「振替が未消化に戻らない場合は〜」と一般化する）。
2. **他教室の名前・事例を出さない**… 「他の教室では〜」を書かない（教室間の情報遮断は本アプリの前提）。
3. **未リリース機能・時期を約束しない**… 「次の更新で直します」「来週対応します」と書かない。
   「要望として記録しました」「確認中です」までにとどめる（§E の裁量を保つ）。
4. **料金・契約・請求に触れない**… 費用や契約条件の質問は「開発者から個別に連絡します」と返す（回答本文で確約しない）。
5. **操作手順は現行の実装で再現できるものに限る**… 推測で手順を書かない。`docs/user-manual.md` または
   `docs/spec-*.md` に根拠がある内容だけを書く（根拠が無ければ「確認して改めて回答します」）。
6. **データの復元・削除など不可逆操作を利用者に指示しない**… 復元・ロールバックは開発者が行う前提
   （`docs/runbooks/rollback.md`）。手順を回答本文で教えない。
7. **個人情報・認証情報を含めない**… メールアドレス・UID・トークン・Storage パス・文書 ID を本文に書かない。

- 上の 1〜7 は **QA 公開（§G-6）の前提条件でもある**（公開は「返答可」よりさらに厳しく、
  **特定の教室でしか成り立たない前提を含まない**ことを追加で満たすこと）。
- 判断に迷うものは**公開しない**（既定 OFF）。基準の改定はオーナー承認事項とする。

### G-5. 返答通知（Q-6）

- 承認済みの回答だけを載せる**軽量コレクション** `workspaces/{ws}/reportAnswers/{answerId}`
  （`classroomId` / 質問の要約 / 回答本文 / `answeredAt` / `readAt`）。
  **教室データ・操作痕跡・送信者情報は載せない**（端末が読む場所なので最小限にする）。
- 表示は **QR 提出通知と同型**（`App.tsx` の起動時通知と実行中通知）。
  - 起動時: 未読（`readAt` なし）の回答があればモーダルで見せる。
  - 実行中: 購読で新着が来たらモーダルで見せる。
  - 既読は **サーバーの `readAt`**（callable `markReportAnswerRead` の部分更新）で持つ。**localStorage は使わない**
    （既読が端末間でずれないため。QR 提出通知で確立した作法・INV-07 の部分更新の原則に倣う）。
- ルール: `reportAnswers` は**その教室のメンバーだけ read**、**write は Cloud Function のみ**。
  ルール反映は `firebase deploy --only firestore:rules`（main マージでは反映されない）。
- 通知は**教室単位**（送信した本人だけでなく、その教室のメンバーが見る）。送信者の個人特定情報は出さない。

### G-6. QA 公開（任意・既定 OFF）

- 公開すると `workspaces/{ws}/qaEntries/{id}` を作る（質問要約・回答本文・公開日時・公開者・タグ）。
- **公開範囲の既定は全教室共有**（仮置き ③）。**元の教室名・生徒名・送信者は載せない**（公開時に開発者が本文を編集する）。
- 読み取りは**教室メンバーなら誰でも可**（write は CF のみ）。教室ごとの出し分けが要るなら
  `visibility: 'all' | 'classroom'` を持たせて既定 `'all'`（構造だけ先に用意し、UI は付けない）。
- 利用者への見せ方は**この段では作らない**（QA 一覧画面・類似 QA の提示は **Q-8 で保留**）。
  公開済みの `qaEntries` は将来 Q-8（類似 QA の即時提示）の材料としてのみ蓄積する。

## H. 蓄積とまとめ読み（ダイジェストパック）

### H-1. 原則（オーナー確定 2026-09-12・変更不可）

- 蓄積先は既存の `developerReports`＋Storage（§C）。**新しい蓄積先を増やさない。**
- 開発者は**必要なときに**（頻度は決めない）蓄積分をまとめて AI に読ませ、仕分け・重複の束ね・既存 Issue との紐付け・
  優先度案・質問の回答案を作る。**AI に渡すデータは制限しない**（本文・操作痕跡＝生徒名を含む・教室データ・
  送信者情報を伏字化せず渡す）。
- **AI の出力は「整理」まで。** 修正の着手判断は §E のまま（オーナー許可後）。AI が Issue コメント・
  ラベル変更・Firestore 書き込みを行ってはいけない（行うならオーナー確認後）。

### H-2. ダイジェストパック（Q-3）

| 項目 | 内容 |
|---|---|
| ツール | `tools/developer-report-digest.mjs`。**読み取り専用**（gcloud トークンで Firestore REST を GET。`tools/lesson-ledger-report.mjs` と同じ作法）。Firestore へは**一切書かない** |
| 入力 | `--since YYYY-MM-DD`（期間指定）または受付番号（report ID）の直接指定。教室での絞り込みは任意 |
| 出力 | Markdown 1 本のパック＋Storage の教室データ（gzip）を**解凍**して同じフォルダへ保存 |
| 収録項目 | **報告文書の全項目**: 受付番号・日時・教室名・種別（bug/request/question）・本文・送信者・報告元（盤面/日程表）と表示条件・アプリ版数・UA・URL・画面・未保存有無・最終保存時刻・操作痕跡（全件）・Issue 番号・回答状況（未回答/回答案あり/承認済み/QA 公開）・スナップショットの保存先 |
| 伏字 | **しない**（オーナー確定） |
| 置き場所 | `local/report-digests/`。`.gitignore` に `local/` を追加する |

- 置き場所を Git 管理外にするのは **公開リポジトリへの流出防止**であって、**AI への提供制限ではない**
  （この区別を実装・スキル文書でも崩さない）。
- パックは**再実行しても同じ入力から同じ出力**になること（決定的。差分を追えるようにする）。

### H-3. AI 用の固定指示文（正本 `src/utils/reportDigestPrompt.ts`）

パック先頭に同梱する固定の指示文。**純関数で生成**し、この 1 箇所を正本にする（ツール・承認画面の
「AI 用プロンプトをコピー」・将来の API 経路で同じ文面を使い、文面の二重管理を作らない）。要件:

1. **役割と範囲**: 整理・要約・案出しまで。コードを直さない・外向きの操作をしない。
2. **やること**: (a) 種別と要点の仕分け、(b) 重複・同一原因の束ね、(c) 既存 Issue／`docs/spec-*.md` との照合、
   (d) 優先度案（影響教室数・データ消失の有無・回避策の有無）、(e) 質問には回答案、(f) 判断できない点は質問として列挙。
3. **回答案を書くときの制約**: **§G-4 の 7 項目を満たす文面にする**（指示文に基準を全文同梱し、AI 提供者が変わっても
   同じ基準で書けるようにする）。
4. **禁止**: 憶測で仕様を断定しない（根拠は `docs/` の該当行を示す）。修正の着手を約束する文面を作らない。
5. **出力形式**: 1 件ごとに「受付番号・種別・要約・推定原因・該当仕様・提案（回答案 or 対応案）・確認したい点」。
6. **データの扱いの明記**: パックには生徒名など実データが含まれる。**外部へ再配布しない**（読ませる相手＝開発者の AI に限る）。

### H-4. 読む入口（ルート A / B / 将来 C）

- **ルート A（推奨・仮置き ①）**: Claude Code スキル `.claude/skills/report-digest/SKILL.md`（Q-4）。
  ツール実行 → パックと `docs/`・未解決 Issue 一覧（`gh issue list`）を読んで**整理結果を画面に表示するだけ**。
  Issue コメント・ラベル変更・Firestore 書き込み・メール送信は**しない**（行うならオーナー確認後）。
  モデル割当は CLAUDE.md に従う（仕分け・要約＝Sonnet 最新／原因の当たり付け・仕様照合＝Opus 最新）。
- **ルート B**: パックの Markdown を任意の AI（claude.ai / ChatGPT / Gemini など）に貼る。**提供者非依存**。
  A と B で**同じ固定指示文**を使う（H-3）。
- **将来のルート C（API 自動処理・Q-7）**: 作らない（仮置き ④）。設計だけ計画
  `docs/plan-2026-09-11-five-requests.md` §3-4 に残す（費用試算・`AnswerDraftProvider` アダプタ・
  全データを API へ渡すと 1 件が数十万トークン規模になりうる点）。**H-3 の純関数を流用できる形**を保つ。

## I. 受け入れ条件（追記分・Q-2〜Q-6。テストで固定）

### I-1. Q-2（種別 `question` の追加）

- モーダル（盤面・日程表**とも同一表示**）の種類は 3 択で、並びが「不具合・おかしい／追加してほしい・要望／使い方の質問」。
- 未選択のまま送ると種別は `bug`（既定は変わらない）。不正な文字列を送っても `bug` に丸められる。
- 質問を選ぶと「すぐには返らない」旨の注意文が出る。bug / request では出ない。
- サーバーは `question` を受理して文書に保存し、クライアント定数とサーバー定数の**種別一覧が一致**する（テストで固定）。
- Issue ラベルが種別で変わる（bug→`type:bug` / request→`type:feature` / question→`type:question`）。
  共通ラベル `status:triage` / `source:user-report` は 3 種別とも付く。
- メール件名に種別の接頭辞が入り、質問は【質問】。`#テスト` を含む質問は **Issue を起票せず**メールだけ届く。
- 日程表 HTML の埋め込みスクリプトが 3 種別を含み、**構文的に妥当**（既存の `new Function` 構文検証テストを通る）。

### I-2. Q-3（ダイジェストパック）

- パックに**報告文書の全項目**が漏れなく出る（操作痕跡の全件・送信者・画面の表示条件・スナップショットの保存先）。
  項目を 1 つでも落とすと落ちるテストを置く。
- 伏字化・除外が**入っていない**（「マスクした」出力になっていないことを固定）。
- `--since` の境界（当日 00:00 JST を含む）と、受付番号の直接指定が両方効く。両方未指定はエラー。
- 出力先が `local/` 配下で、`.gitignore` により Git 管理外（`git check-ignore` 相当の確認）。
- Firestore への書き込み API を**呼ばない**（読み取り専用であることをテストで固定）。
- 同じ入力から同じ出力（決定的。日時など可変部分は入力の値に由来するもののみ）。

### I-3. Q-4（スキル `report-digest`）

- スキル手順に「表示のみ・外向き操作はオーナー確認」が明記され、Issue 書き込み系コマンドを含まない。
- 開発用教室 `v8OZ7zH8vONNHjjYVcR1` の報告で dry-run が通る（本番教室へは読み取りのみ）。

### I-4. Q-5（承認画面・callable）

- 状態遷移は §G-3 の表どおり（純関数でテスト）。承認済み → 未回答へは戻せない。
- **`answerDraft`（回答案）は利用者向けのどの経路にも出ない**（`reportAnswers` に載らない）。
- 「承認して返答」は §G-4 のチェックを全部満たさないと押せない（未確定の間は暫定チェックとして実装し、
  基準が確定したら文言を差し替える）。
- callable は `requireDeveloperMember` 必須。非開発者は拒否。入力（本文長・必須）を検証する。
- 「QA として公開」は**既定 OFF**で、押したときだけ `qaEntries` を作る。教室名・生徒名が本文に残っていない。
- 回答本文の更新は `answerRevision` を +1 し、**既読を未読に戻さない**。

### I-5. Q-6（返答通知）

- 起動時に未読（`readAt` なし）の承認済み回答だけがモーダルに出る（`selectStartupSubmissionsToNotify` 同型の純関数）。
- 実行中に新着が来たらモーダルに出る。既読は callable の**部分更新**で付き、他フィールドを消さない。
- 既読状態は**サーバー保持**。別端末で開いても既読が引き継がれる（localStorage を使わない）。
- `npm run test:rules`: 他教室のメンバーは `reportAnswers` を読めない。クライアントからの write は全て拒否。
- 回答通知に教室データ・操作痕跡・送信者が含まれない。

### I-6. 横断（3 種別共通）

- §E の「勝手に修正を始めない」が質問・要望にも適用される（Issue 本文の注意書きが 3 種別とも入る）。
- 既存の bug / request の送信・メール・Issue 起票の挙動が**変わらない**（既存 §F の受け入れ条件は全て維持＝回帰テスト）。
