# 確定仕様：振替ストック（To-Be 正本）

> 全体見直しセッション（2026-06-07）でオーナー確認。
> **2026-06-08 Phase 4 精査で重要訂正**：現行の `autoShortage` は「月回数−実配置」ではなく
> **「休日（休校曜日/個別休日）で潰れた通常授業を自動で振替（未消化）に計上する仕組み」**だった。
> これは有用なので**残す**（当初の「autoShortage廃止」は撤回）。

## 1. 発生（積まれる）

### A. 休日振替（自動・★残す）
- **日付をクリックして休日（holidayDates）にすると、その日に通常授業があった生徒は全員「休日振替」として未消化に自動計上**される（理由ラベル `休日振替`/`定休日振替`）。
- ★**計上タイミングは過去・未来を問わず、休日設定した時点で即時**（旧実装は「その日が過ぎてから」だったが、即時計上に変更）。
- 休校曜日（closedWeekdays）由来の潰れた通常授業も同様に振替計上。`forceOpenDates` は除外。
- 集計単位は **生徒 × 科目**。

### B. 欠席登録（明示・★実装済み）
- 盤面の通常コマの生徒を **「欠席」登録すると、その生徒×科目の振替ストックに1コマ積まれる**（休日以外の個別欠席用）。
- 欠席登録した**コマは盤面から消さず「欠席」表示で残す**。席は**ロックせず、クリックで別生徒・体験・メモを追加できる**。
- 振替元として日付・時限を記録。理由ラベルは持たない。
- ※**2026-06-08 精査で実装済みと確認**。生徒メニュー「休み」ボタン（`handleMarkStudentAbsent`）が本要件を満たす。振替なしの欠席は別ボタン「振無休」（absent-no-makeup）。詳細は `docs/SESSION-HANDOFF.md` §0。

### C. 繰越
- 運用開始時の繰越は、初期設定の「未消化振替」で登録した分が最初から積まれる（`docs/spec-save-restore.md` §6）。

## 2. 消化／戻し

- ストックを選んで**空きコマへ置くと、残が1減る。**
- 置いた振替を**盤面から外す（ストックへ戻す）と、残が1増える。**

## 3. 表示・ルール（現行から維持）

- 集計単位は **生徒 × 科目。**
- 振替ストックはコマ表操作後も**開いたまま維持**する。
- **手動追加した生徒は振替ストックにカウントしない。**
- 振替中／移動中に**同じコマへ同じ生徒が既にいると配置を拒否**し、選択状態は維持する。

## 4. 廃止する概念

- ★**マイナス残（希望回数を超えて配置したときの過剰配置表示）は廃止**。残数は 0 を下限とし、マイナスにしない（`overAssignedRegularLessons` による減算・`negativeReason` を撤去）。
- ~~autoShortage の自動計算~~ → **残す**（§1-A）。

---

## ★ 実装メモ（Phase 4）

現行は仕様に近い。3点とも対応済み（A/C=PR #15、B=既存実装を精査で確認）：
1. **A 即時計上**（✅）：`computeAutomaticShortageOrigins` の「今日まで」制限（todayKey キャップ／pastScheduledDates）を外し、休日設定した未来日も即時に振替計上する。
2. **B 欠席登録**（✅ 既存実装・2026-06-08 確認）：盤面コマの生徒を欠席登録→振替origin生成＋「欠席」表示（コマ残す・席操作可）。生徒メニュー「休み」ボタンが該当。
3. **C マイナス残廃止**（✅）：`balance` の `overAssignedRegularLessons` 減算と `negativeReason` を撤去し、残数を 0 下限に。

## ★ 移行（本番反映）時の必須チェック ※オーナー強指定

**既に本番運用中。本番の振替残数に直結する。**
- A（即時計上）と C（マイナス残廃止）は**意図的に残数を変える**変更。特に A は未来休日を持つ教室で残数が増える。
- 反映前に**教室ごとに変更前後の残数を読み取り専用で計測・差分確認**し、想定どおりのズレであることを確認してから本番反映する。
- 書き込み検証は開発用教室 `v8OZ7zH8vONNHjjYVcR1` のみ。

### ✅ 差分検証の結果（2026-06-09・読み取り専用）

最新の本番自動バックアップ（`workspace-auto-backups/main/hourly/2026-06-08T23.json`）を入力に、OLD（=本番 v1.5.289 相当・4A/C 前）と NEW（=main・4A/C 後）の `buildMakeupStockEntries` を**同一入力**で実行し、生徒×科目ごとの `balance` を比較（today=2026-06-08 固定）。

| 教室 | balance 合計 OLD→NEW | 変化キー数 | 備考 |
|---|---|---|---|
| 日大前校（本番） | 112 → 112 | 0 | 変化なし |
| 薬円台校（本番） | 0 → 0 | 0 | 未来休日なし |
| 緑が丘校（本番） | 50 → 50 | 0 | 変化なし |
| 開発用教室 | 101 → 102 | 1 | **C:** 戸髙咲耶/算 −1→0（マイナス残クランプ）|

- **A（即時計上）：全教室で差分ゼロ。** 唯一の未来休日が お盆（2026-08-10〜15）だが、現行 SY2026 テンプレの有効期間が **その後に開始**（日大前/開発用=09-07・緑が丘=08-31・薬円台=未来休日なし）。在籍期間が未来休日と重なる通常授業が 0 件のため、未来休日の即時計上は発生しない。
- **C（マイナス残廃止）：本番3教室は影響ゼロ**（OLD でマイナス残のキー 0 件）。開発用教室の 1 件のみ −1→0。
- **結論：このスナップショット時点では、本番3教室の振替残数はデプロイで一切変わらない。** 変わるのは開発用教室の 1 キーのみ。
- ★**時点依存の注意**：A が無影響なのは「未来休日がテンプレ有効期間より前」という現データ条件による。今後 SY2026 有効期間内（2026-09〜2027-03）に授業曜日と重なる休日を設定すると、その時点で即時計上が発生する（仕様どおりの挙動）。デプロイ自体は現残数を変えないが、新挙動は以後の休日設定で顕在化する。
- 検証ハーネスは使い捨て（`.verify-tmp/` と `*.verify.test.ts`・`makeupStock.old.verify.ts`）でコミットせず実行後削除。本番データ書き込みは一切なし（GET/ダウンロードのみ）。

## ★ 下流ブロックへの影響（覚書）

- **生徒日程表の通常回数（ブロック⑨）**：欠席で振替化した回の見せ方を、本仕様（欠席表示でコマ残す＋ストック＋1）に合わせて再確認する。
