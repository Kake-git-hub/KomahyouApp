# 確定仕様：横断保証(INV)台帳（正本）

> オーナー承認済み（2026-07-11 制度化合意）。台帳の管理者＝**spec-curator**。
> 追加・文言改定は**オーナー承認**を要する。上限 15〜20 件。

## 位置づけ（この台帳は何か）

この台帳は、`spec-index.md` の 12 ドメイン正本（教室権限／保存・復元／振替／講習／…）と**直交する
「横断保証（INV = Invariant）」の層**である。ドメイン正本が「この機能はどう動くべきか（機能仕様）」を
定義するのに対し、本台帳は「**経路や実装が変わっても崩れてはいけないユーザー体験の保証**」を、
再現経路でなく**保証単位**で 1 行に固定する。

同じユーザー体験のバグが、経路（トリガ）を変えて繰り返し再発してきた——という実績が制度化の理由である。
例：講師日程表の二重表示は「生徒を別講師の机へ移動」（v1.5.388）で直した後、「講師 D&D の入替（swap）」
（v1.5.436）という別経路で再発した。バグを**再現経路でなく保証（INV-01）単位で固定**していれば、
2 件目の経路も同じマトリクスの空セルとして先回りで塞げる。

### 設計原則（この台帳の効き方）

- **バグを減らすのは INV の数ではない。** 効くのは「**テストで強制された INV**」×「**その保証の事故頻度**」。
  書いただけ（テストで守っていない）の INV はゼロ円。
- **効果は強く逓減する。** 最初の 10 件ほどで痛みの 8〜9 割を抑える。件数を増やすほど限界効果は小さい。
- **正しいスケールは横でなく縦。** INV の**数を増やす（横）**のではなく、各 INV の**操作マトリクスの網羅と、
  帰属・消化などの判定を担う権威関数への一元化（縦）**を深める。
- **守備範囲は「回帰」と「経路違い再発」のみ。** 性能・UX の分かりにくさ・新機能の仕様漏れは本台帳の対象外で、
  **spec-curator ＋ 実機確認**の仕事である。

### 重複ゼロ原則（ドリフト防止）

- **保証文は本書（`docs/spec-invariants.md`）に 1 回だけ書く。**
- ドメイン正本（`docs/spec-*.md`）と `memory/` は、保証文を複製せず **INV-ID で逆参照**する。
- 3 箇所へ同じ保証文を複製することは、`6793374`→`2dce7b4` 型の**巻き戻りドリフトの温床**として**禁止**する
  （元の同一性補完が別マージの spread 単純化で消えて再発した実例。§ INV-05）。

---

## 運用ルール（正式）

### 1. 台帳の管理

- 正本は本書。**上限 15〜20 件**（横に増やさない設計原則の運用上の歯止め）。
- 台帳への**追加条件は次の 2 つのみ**：
  1. **実際にユーザー体験のバグが起きた**（同種の再発を保証単位で固定したい）。
  2. **spec-curator が新機能でリスクの高い面を特定した**（着手前の予防的追加）。
- 追加・文言改定の権限は **spec-curator**（オーナー承認のもと）。個々の実装者が勝手に文言を薄めない。

### 2. 2 層制（強制 INV と準観察 INV）

- **強制 INV**（INV-01 / 02 / 03 / 06 / 07 / 08 / 09）：**操作マトリクステスト 1 ファイルを義務**とする。
  - ファイル名に INV-ID を含め、拡張子は `*.matrix.test.ts`（例：`inv01-teacher-attribution.matrix.test.ts`）。
  - 「経路違いの再発を 2 回以上、実績として確認済み」の INV が強制。
- **準観察 INV**（INV-04 / 05 / 10 / 11）：**非強制**。既存の経路テストで守るにとどめる。
  - **two-strike で昇格を検討**：経路違いの再発の**2 件目**が出た時点で、強制への格上げ（マトリクス新設）を
    **検討する契機**とする。**昇格の実施可否はオーナーが判断する**（自動昇格ではない。マトリクス 1 ファイルは
    以後ずっと維持コストを負い、上限 15〜20 件の設計原則＝「横に増やさない」とも競合するため）。
    見送った場合は、該当 INV 節に**判断と日付、および代わりに何で担保するか**を残す（記録なき見送りは不可）。
- 仕様が未決のセル（現実装と裁定が乖離しているマス等）は **`it.todo` で可視化**し、CI は緑を維持する。

### 3. 修正フローの完了定義（UX 系バグに必須・内部リファクタ／文言のみは免除）

UX に影響するバグを直したら、以下 4 点を満たして初めて「完了」とする：

1. **どの INV 違反かを特定**する。該当 INV が台帳に無ければ、**台帳追加が先**（§1 の追加条件）。
2. **経路テスト＋該当 INV のマトリクス拡張**（今回の経路のセルを埋める／薄い列は再確認する）。
3. **兄弟監査**：該当 INV のマトリクス行を読み下ろし、**隣接操作**（移動⇄入替⇄削除／生徒⇄講師／
   盤面⇄日程表／全ルールの列挙）に**穴が無いか**確認する。1 経路だけ塞いで隣で再発、を防ぐ。
4. **CHANGELOG・コミットメッセージに INV-ID を記載**する（違反履歴が台帳へ蓄積される）。

### 4. 分類ルール（症状でなく真因構造で分類）

- 各バグに **primary INV を 1 つ**割り当てる（必要なら secondary を追加）。
- **症状でなく真因構造で分類**する。例：
  - 同一操作が複数回**再実行**される → **INV-03**。
  - 別処理が状態を**上書き**する → **INV-02**。
  - **警告・割振対象判定**が実態と食い違う → **INV-09**。

### 5. CI（ブロッキング・改定ガード）

- マトリクステストは**毎 push ブロッキング**（既存 unit スイートに相乗り。`ci-tests.yml` の `unit` ジョブ）。
- **改定ガード（`inv-guard` ジョブ・実装済み）**：`*.matrix.test.ts(x)` を変更する差分は、**同一 push 内で
  `docs/spec-invariants.md` の改定を伴わなければ CI で落とす**。マトリクスの assert 薄化（＝ INV の格下げ）を、
  台帳の追認なしに通さないためのガード（`.github/workflows/ci-tests.yml`）。

### 6. regression-reviewer ＝ INV 監査ゲート（4 点出力）

リファクタ・マージ衝突解決の後などに、`regression-reviewer` が読み取り専用で以下 4 点を出力する：

1. **差分が触れる INV の特定**＋コミットの INV-ID 記載チェック。
2. **兄弟被覆の検証**（隣接操作に穴が無いか）。
3. **INV 格下げの検知**（マトリクスの assert が薄まっていないか）。
4. **権威関数の分散検知**（帰属・消化などの判定が複数箇所に散っていないか）。

### 7. 権威関数の一元化（別タスク・マトリクス先行）

- 講師帰属の判定（`resolveDeskTeacherIdentity` 等の一元化）は**別タスク**として中期に実施する。
- **マトリクスを先に整える**（INV-01 マトリクスに守られた最初の改修として着手する）。これにより、
  一元化リファクタが帰属一意（INV-01）を巻き戻さないことをマトリクスが保証する。

---

## INV 各論

各 INV は **保証文 ／ 層 ／ 仕様上の例外・確定仕様 ／ 違反履歴 ／ マトリクステストファイル ／ 担保状況**
を持つ。違反履歴の版数・commit・日付は、統合バグ台帳から**転記**したもの（捏造せず、根拠のある版のみ）。

---

### INV-01【強制】講師帰属の一意性

**保証文**：講師日程表では、生徒の各コマは「盤面で実際に置かれた机の講師」1 名のみに表示される。

- **層**：強制（経路違い再発を 2 回以上実証）。
- **確定仕様**：置いた机の講師のページに **1 回だけ**出て、旧担当（基本データ上のテンプレ講師）や
  別の机の講師のページに**漏れも二重表示もしない**（両方向のアサート）。
- **違反履歴**（transcribe）：
  - `5395a05` / 2026-05-30 … `regularTeacherIds` フォールバック導入（＝温床の起点）。
  - `6c5ddf0`・`776260c` / 2026-05-30 … 混在割当・マッチング全般。
  - **v1.5.388** / `81d5202` / 2026-07-04 … 同コマ内で生徒を別講師の机へ移動。移動で机の `teacherId` が
    消え、基本データ行の旧講師 ID が残り旧講師へ二重表示（`sameDayMoveSourceDate` 除外ガードで修正）。
  - **v1.5.436** / `09724e6` / 2026-07-11 … 講師 D&D の入替（swap）。テンプレ配置由来で
    `teacherAssignmentTeacherId` を持たない机同士の swap では生徒が動かず v1.5.388 のガードが不発火。
    `computeTeacherMove` が着地机の講師名→ID を補完して修正。
  - **2026-08-07** … **出欠記録だけが残る机**（`lesson` 無し・`statusSlots` あり）が「空き机」と判定され、
    テンプレ足場講師に `teacher` を上書きされた。記録は机に残るため、元講師のコマは空白／テンプレ講師の
    ページに他人の生徒が出る（報告: 緑が丘校 8/6 4限 加藤→井上。本番実測 日大前 12 件・緑が丘 3 件）。
    `mergeManagedWeek` の再付与ループ（`consumedManagedTeacherIndexes` と `targetDesk` 選定）に
    `hasRecordedStatusSlots` を追加して修正。**兄弟監査で盤面側の同型ルート**
    （`repackTeacherOnlyDesks` の詰め直し／講習の講師自動割当の空き机判定）も同時に塞いだ。
- **構造対策＝日程表は盤面の写し（オーナー確定 2026-08-07・全教室で有効）**：上記のズレは、
  **日程表だけが通常授業テンプレを読み直して盤面を重ね直している**（`buildScheduleCellsForRange` →
  `mergeManagedWeek`。盤面自身はこの再マージをしない）ことに由来していた。オーナー指示「必ず盤面と
  日程表がそろうようにして」に基づき、**日程表は盤面をそのまま描く**（`boardOnly`＝
  `featureRollout.boardOnlyScheduleCells`。開発用教室で先行 v1.5.472 → 同日 全教室へ昇格）。
  - テンプレは日程表へ渡す盤面を作る `ensureWeeksCoverDateRange` が**未生成週の生成時に既に読んでいる**ため、
    再マージを外しても未来週は空白にならない（＝再マージはテンプレの二度読みだった）。
  - 唯一の体感差は「基本データを直しても**盤面を開いて反映するまで日程表に出ない**」。
  - **「テンプレにだけ残る通常授業が日程表に湧く」件は盤面が正**（オーナー裁定 2026-08-07。実測1件＝
    日大前 8/6 5 限 絹川・山口千陽）。
  - **INV-02 の「非 manual 講師＝テンプレ追従」との関係**：追従の対象は生徒のいない講師だけの机に限られ、
    講師日程表はその机を元々表示しないため、表示上の衝突は無い（本番実測の差 1,262 件はすべて不可視）。
  - **`boardOnly=false` の旧経路（`overlayBoardWeeksOnScheduleCells` / `mergeManagedWeek`）はロールバック用に
    残す**。挙動は `inv01-teacher-attribution.matrix.test.ts` の操作 7 で固定してある。**フラグを
    development-only へ戻さないこと**（戻すと「盤面にあるのに日程表に無い」が再発する）。
- **マトリクステストファイル**：`src/components/schedule-board/inv01-teacher-attribution.matrix.test.ts`
  （**実装済み・25 テスト**。操作＝配置(新規)/移動/入替(講師swap)/席まるごと入替(seat swap)/削除(講師)/
  生徒swap × 確認点＝直後/テンプレ再マージ後/serialize 往復）。
  - **席まるごと入替（seat swap・v1.5.453 追加）**：講師D&Dで生徒が絡む机へドロップしたとき、選択モーダルで
    「講師だけ入替」か「生徒ごと席入替」を選べる（`computeTeacherMove` の `swapMode`。オーナー要望＝席番号と
    講師生徒のペアを任意に合わせたい）。seat モードは講師と生徒のペアを保ったまま席(desk.id)だけ交換するため、
    帰属は**移動先講師＝元のペア講師**になる（講師だけ入替＝ペアが崩れて移動先の別講師に付く、とは逆）。
    どちらのモードも各生徒は1机の講師にのみ表示され二重表示しないことをマトリクスで担保。
  - **丸ごと振替（2026-08-02・Issue #40）**の講師帰属（講師ブロック移送＝manual 固定＋`backfillDeskTeacherAssignmentId`
    による ID 補完・振替元／上書きされた振替先の講師机は削除 tombstone）は、2日にまたがる専用 fixture が要るため
    `inv06-whole-day-transfer.matrix.test.ts` 側で固定する（本ファイルとは重複させない）。
  - **テンプレ足場講師の日単位抑止（2026-08-03・オーナー指示）**：丸ごと振替した両日は、テンプレ由来の足場講師を
    再付与しない（`buildTemplateTeacherSuppressionKey` を `suppressedRegularLessonOccurrences` に相乗りさせ、
    overlay が該当日の管理セルから「生徒のいない机の講師」を落とす）。**丸ごと振替した日だけの例外**で、
    それ以外の日のテンプレ追従（下記 INV-02 の確定仕様）は不変。詳細は `spec-makeup-stock.md` §B-2-3。
- **担保状況：○**（経路ロックが厚く、マトリクス新設済み。既存の `scheduleHtml.test.ts` /
  `ScheduleBoardScreen.test.ts` の経路テストと二重で守る。`5395a05` のフォールバックは非改変で維持）。

---

### INV-02【強制】手動編集の永続化

**保証文**：盤面への手動編集（配置／削除／移動／入替／科目選択／出欠入力）は、自動処理
（テンプレ再マージ／自動割当／詰め直し／リロード）で巻き戻らない。

- **層**：強制。
- **仕様上の例外（オーナー裁定 2026-07-11）**：**テンプレの反映日を決めて適用したときのみ**、反映日以降は
  テンプレが正となり上書きする（ユーザー配置も対象。置かれていた振替・講習は未消化ストックへ返却。
  反映日前は凍結＝INV-10）。それ以外のときは**ユーザーの配置が正**。
- **裁定の実装形（2026-07-11 実地調査＋オーナー確定・コード無変更で決着）**：ユーザーが講師を置く**全経路**
  （講師セル選択／削除／生徒移動で空いた机のピン留め／講師 D&D／QR 講習の自動割当）は `manualTeacher=true` で
  記録され、日常の自動処理で不可侵＝「ユーザーの配置が正」は**実装済み**。**非 manual 講師＝テンプレが自動で
  置いた足場講師のみ**で、これは**テンプレに追従する（基本データ・テンプレ編集で盤面の足場講師が変わる／
  消えるのは正）が確定仕様**。当初の「既知の乖離（手動配置講師が再マージで消える・要修正）」は**前提誤り**として
  解消（そのような経路は存在しない）。マトリクスの旧 `it.todo` は講師帰属境界の仕様ロックテストへ置換済み。
  訂正：`ScheduleBoardScreen.test.ts:2074` は manual 講師の生存方向のみをロックしており、当初の
  「非 manual クリアをロック中」という記載は誤りだった。
- **違反履歴**（transcribe）：
  - **v1.5.435**（削除講師 tombstone・2 経路）：
    - `b8b075f` / 2026-07-10 … `repackTeacherOnlyDesks` が tombstone（`teacher=''`, `source='deleted'`）を
      空机とみなしクリア→テンプレ再マージで復活（日大前・落合/永山が 7-25 全限×でも赤配置）。
    - `1699788` / 2026-07-11 … 起動毎の `autoAssignTeacherToSpecialSession`（講習の講師自動割当）が同じ
      tombstone を空き机とみなし上書き（敵対検証で追加発覚。repack 側だけ直しても再発）。
  - **v1.5.396**（講習配置の stale 書き戻し）：
    - `a0dba9f` / 2026-07-06 … 起動時の未保存ローカル書き戻しが版数ゲートを素通り（`savedAt` 壁時計比較のみ）し、
      別端末の後保存を古いローカルで上書き。提出済み本人の講習配置が約 4.5 分後に消え残数復活（本番障害）。
    - `9bafd01` / 2026-07-06 … 未提出配置除去 effect が空 Set 初期化で初回に既存未提出生徒まで誤除去。
    - v1.5.405 / 2026-07-07 … 「保存し忘れ救済」（起動時書き戻し）を経路ごと**完全撤去**（復活禁止・
      起動時は常にサーバー最新が正）。
  - **v1.5.349** / 2026-06-29（空机講師名クリア）：生徒移動で移動元机が 0 人になると、`mergeManagedWeek` の
    自動割当講師クリア分岐が講師名まで消す（`computeStudentMove` で移動元 0 人でも講師名保持に修正。
    swap は相手生徒が入るため対象外）。
  - pack-sort 出欠実績：`packSortCellDesks` が `statusSlots[0]`（出席/休み/振無休の実績）を空席扱いで
    巻き込み再配置。同型の左詰めが `mergeManagedWeek` 側にもあり単体修正では再発しうる。
  - **非 manual 講師クリア**（2026-07-11 仕様確定で解消・INV 違反ではなかった）：memory が「手動配置講師が
    消え生徒だけ残る（未修正）」と記録していたが、実地調査で (a) 手動配置は全経路 manual 扱いで消えない
    (b) `mergeManagedWeek` に「講師だけ消え生徒が残る」を起こす分岐は無い（lesson 保持分岐は teacher を維持し、
    クリア分岐は lesson も落とす）ことを確認。テンプレ足場講師のテンプレ追従は仕様（上記）。
    残る実害＝出勤不可テンプレ講師の赤置き直し（v1.5.435 は削除済み講師のみ対策）は別課題として Issue 起票。
- **マトリクステストファイル**：`src/components/schedule-board/inv02-manual-edit-persistence.matrix.test.ts`
  （**実装済み・29 テスト**。旧 `it.todo` 1 件は 2026-07-11 の仕様確定を受けて講師帰属境界の仕様ロック 4 件
  ＝テンプレ足場は追従（クリア／再付与の両方向）・manual 配置は不可侵・QR 自動割当（schedule-registration）も
  不可侵、へ置換済み。2026-08-02 に「丸ごと振替で講習期間外へ移した提出講師を起動時自己修復が置き直さない」
  1 件を追加）。
  - **違反履歴（2026-08-02・丸ごと振替 Issue #40 の追随／オーナー確定）**：起動時の自己修復
    `reconcileSubmittedTeacherPlacements` の「配置済み」判定が**講習期間内のセルだけ**を走査していたため、
    丸ごと振替で QR 提出講師の机を期間外へ意図的に移すと「未配置」と誤判定し、起動毎に期間内へ置き直して
    **手動の移動が巻き戻る**（移送先にも残るため同じ講師が2か所に見える）。判定を**盤面全体**へ広げて修正
    （揮発＝どこにも居ないケースの復旧は従来どおり）。★**期間内限定に戻すと再発**（mutation で 2 件落ちる）。
- **テンプレ足場の追従に対する唯一の例外（★オーナー指示 2026-08-03）**：**丸ごと振替した両日**だけは
  テンプレ足場講師を再付与しない。丸ごと振替が「その日の姿ごと別日へ移す」操作で、講師の顔ぶれも
  ユーザーの操作結果として固定するのが確定仕様のため（実測でリロード後に 振替元=空／振替先=移送講師＋
  その日のテンプレ講師 となり表示が揃わなかった）。抑止は日単位キーで表現し、**それ以外の日は従来どおり追従**する
  （`inv06-whole-day-transfer.matrix.test.ts` に「抑止していない日では従来どおり付く」ロックあり）。
  テンプレ上書きの反映日以降は抑止も解除され追従へ戻る。詳細は `spec-makeup-stock.md` §B-2-3。
- **担保状況：○**（手動編集＝配置/削除(講師)/移動/入替/科目選択/出欠 × 自動処理＝再マージ/講習自動割当/詰め直し/
  リロードのマトリクスを網羅。講師帰属境界（テンプレ足場は追従・ユーザー配置は不可侵）も仕様ロック済み。
  **丸ごと振替（2026-08-02・Issue #40）× 再マージ**〔振替元に通常・講師が復活しない／振替先の移送結果が
  巻き戻らない／テンプレ足場講師が両日とも湧かない〕は `inv06-whole-day-transfer.matrix.test.ts` 側で固定
  ＝本ファイルとは重複させない）。

---

### INV-03【強制】操作の冪等（1 操作 1 適用）

**保証文**：1 回の操作は 1 回だけ適用される（リロード／再マウントで再実行されない）。

- **層**：強制。
- **分類注記**：INV-03 ＝ **同一操作の複数回適用（再実行）**、INV-02 ＝ **別処理による状態上書き**。
  症状でなく真因構造で分類する。
- **違反履歴**（transcribe）：
  - **Issue #46** / `bd0865d` / **v1.5.406** / 2026-07-07 … `studentScheduleRequest(mode:unassign)` が処理後も
    App 永続 state に残り、重複ガードがローカル `useRef` のため盤面 key 再マウントで消え、古いコマンドが
    再処理（組み直した講習が未消化へ戻る）。
  - `b617fb2` / **v1.5.421** / 2026-07-09 … 同型構造の `teacherAutoAssignRequest` で再発。門田/角田ら講師が
    再マウント毎の再発火で再削除される（緑が丘 8/4 の本番障害として顕在化）。
- **マトリクステストファイル**：**未整備（義務・two-strike 済み）**。現状は経路テスト
  `ScheduleBoardScreen.test.ts`（`shouldProcess*` / `consume*` で「処理後 state を null 化消費・
  再マウントをまたいで 1 回だけ処理」を純関数で固定。修正なし＝3 回／修正あり＝1 回）で暫定担保。
  **次の違反時にマトリクス化**する。
- **担保状況：○**（生徒・講師の両一過性リクエストを純関数で固定済み。処理後に state を消費(null 化)＋
  判定純関数化で再発火を封じている）。

---

### INV-04【準観察】日程表の経路一致

**保証文**：生徒日程表・講師日程表は、同じ盤面からどの経路（直接表示／ポップアップ／別タブ／最新表示）でも
同じ内容を表示する。

- **層**：準観察（非強制）。**two-strike で昇格**。
- **準拠（非強制）**：PDF・共有ビューも同内容であるべきだが、内容乖離の実バグが出た時点で強制へ昇格する
  （実バグ史では PDF＝体裁のみ／共有＝教室混同で INV-08 側）。
- **違反履歴**（transcribe）：
  - `13eb818` / **v1.5.294** / 2026-06-11（Issue #28）… テンプレ非掲載の盤面通常授業が生徒日程表から脱落
    （テンプレ照合ロジックがテンプレ非掲載の盤面授業を除外）。
  - `d2a7e38` / 2026-04-15 … 振替欄空白（2 原因合成）。
  - `49aec4d`・`b2c481c` / 2026-06-09 … 埋め込み JS のエスケープ崩れ／空フォーマット崩れで日程表が全停止。
  - `bd92edd` / 2026-06-22 … ポップアップ同期がトークン確保待ちで gate 遅延。
  - **v1.5.342** / 2026-06-28 … 「最新表示」を押しても割振結果が反映されない（別経路残存）。
  - `664e3fa` / **v1.5.400** … `subjectDurations` の payload 欠落（`subjectSlots` だけ載る非対称）。
  - v1.5.343 / 2026-06-29 … `schedule-student-count-save` の `subjectDurations` 反映漏れ。
  - **Issue #42**（2026-07-11 時点 **OPEN・未修正**）… App.tsx と ScheduleBoardScreen の二重同期で payload が
    非対称（fingerprint dedup の最後勝ちで欠落）。
- **マトリクステストファイル**：準観察のため未整備。経路テスト `scheduleHtml.test.ts`（生成 HTML の script を
  `new Function()` で構文検証／盤面実配置を根拠にセル描画／`subjectDurations`・提出日時を payload へ
  シリアライズ）で担保。
- **担保状況：△**（**Issue #42 が OPEN**。App.tsx と ScheduleBoardScreen の payload フィールド一致検証が未着手。
  ここで内容乖離の 2 件目が確定すれば強制へ昇格）。

---

### INV-05【準観察】回数表示の実配置一致

**保証文**：日程表の授業回数は盤面実配置・提出データと一致する。対象＝実施/予定の授業回数表示
（在庫残数は INV-06）。

- **層**：準観察（非強制）。**two-strike で昇格**。
- **違反履歴**（transcribe）：
  - `6793374`（元修正）→ `2dce7b4`（巻き戻り）→ `4dc2b87` / **v1.5.294** / 2026-06-10（再修正）…
    `mergeManagedDeskLesson` の生徒同一性補完（`managedStudentId` 等の managed 側優先補完）が、後続マージの
    `{ ...managedStudent, ...student }` 単純 spread に巻き戻り、生徒日程表で盤面配置済みの通常授業が
    回数表にカウントされず実態より少なく表示された。**CLAUDE.md 回帰防止ルール制定の直接の引き金**。
  - **v1.5.468** / 2026-08-05 … **希望数の正本が二重に減る／予定側が種別で非対称**（オーナーからの
    「通常・講習の回数と希望数を整理したい」に端を発する棚卸しで発見。2 件とも回数表示が実態と食い違う）。
    - **二重減算**：在庫由来(session)講習の単発削除が、提出データ `subjectSlots` −1（`74c830d` で追加）と
      表示調整 `scheduleCountAdjustments` −1（それ以前からの経路）の**両方**を積み、1 回の削除で希望数が
      2 減った。新機能追加時に**旧経路を外し忘れる**古典型。純関数 `resolveDeletedStudentCountAccounting`
      （判定は `isSessionLectureDeletion`）が **2 つの帳簿の行き先を 1 つの戻り値
      `{ nextAdjustments, decrementSubjectSlots }` で決める**形にして解消（呼び出し側は配るだけ。
      戻り値を分解して片方だけ別経路で取り直すと同型事故が再発する）。
    - **予定側 +1 の非対称**：`buildSerializedScheduleCountAdjustments` の +1 が**講習だけ**に効いており、
      手動追加の増コマは実績だけ増えて警告が出っぱなしだった。**増コマ(extra)にも** +1 を広げ、
      数える条件を実績側と対称化（trial 除外／`studentSlots`＋`statusSlots` の**両走査**／absent・moved 除外）。
      ★出欠を付けるとコマは `statusSlots` へ**移る**ため、片走査だと出席の瞬間に +1 が消える（INV-06 と同型）。
      ⚠️**手動追加の通常・振替は対象外のまま**（当初は全種別へ広げる方針だったが、本番実データの事前実測で
      基本データの同じ枠と重なる手動追加通常が 日大前 173/223 件・緑が丘 208/269 件、うち相殺なしが
      54/68 件あり、+1 すると予定が二重に増えて**逆に新規警告が出る**と判明したため範囲を絞った。
      広げるにはテンプレ予定との重複排除が要る＝オーナー判断待ち）。
- **マトリクステストファイル**：準観察のため未整備。経路テスト `ScheduleBoardScreen.test.ts`
  （`managedStudentId` 欠落でも管理データ側 ID を保持＝96 行の回帰テスト／希望数の正本の排他 3 件）と
  `scheduleHtml.test.ts`（手動追加コマの予定側 +1 の対称性 6 件・回数表の呼称 1 件）、
  `scheduleViewData.test.ts`（**削除後の希望数を実数値で固定**＝在庫由来講習を1コマ削除しても希望は
  1 しか減らない。★希望 3→残 2 の規模で書くこと。希望 1→残 0 だと `applyCountAdjustments` の 0 クランプで
  行ごと消え「希望＝実績」に化けて二重減算が素通りする＝実測で確認）で担保。
  希望回数 −1（`scheduleCountAdjustments`）を積む経路は 削除／全コマ削除に加えて**丸ごと振替の振替先処分**
  （2026-08-02・Issue #40）が増えた（全コマ削除と同じ `returnedEntryIds` スキップ規則。
  `inv06-whole-day-transfer.matrix.test.ts` で回数調整も assert）。
- **呼称の正本**（2026-08-05 オーナー確定）：括弧内・警告文は出どころで呼び分ける。
  **通常＝「予定数」**、**講習＝「希望数」**（QR提出・室長登録の `subjectSlots` **± 調整**）。
  両方を「希望数」と書かない。**講習側の定義は今後も変わらない**（提出データが正本）。

- **★通常の予定数の算出方式を「テンプレ由来」から「盤面ベース」へ移行中**（オーナー確定 2026-08-05・段階導入）：

  | | 旧方式（テンプレ由来・移行前） | **新方式（盤面ベース・移行先）** |
  |---|---|---|
  | 予定数の根拠 | `buildExpectedRegularOccurrences`（基本データを曜日展開・**盤面を見ない**） ± `scheduleCountAdjustments` | **その期間の実績 ＋ その期間の未振替の休み**（`computeOutstandingAbsenceOrigins`・盤面だけが根拠） |
  | 出欠記録が無いとき | テンプレと盤面がずれていれば警告 | **必ず一致し警告が出ない**（これが新仕様の芯） |
  | 期間外へ振替したとき | 元の期間に警告が残り続ける | **元の期間の予定数からも減る**（7月に休み・8月に振替なら 7月は `3(3)`） |
  | 削除したとき | 表示調整に −1 を積んで辻褄を合わせる | **盤面から消えるので自動的に減る**（帳簿が要らない） |
  | 組み忘れ検出 | `0(4)` で拾える | **拾えない**（置いていなければ行ごと消える。オーナー受け入れ済み） |

  - **確定した仕様上の割り切り**（オーナー承認済み・「バグではない」）：組み忘れ検出を失う／
    「組み過ぎ」「二重取り（休み解除したのに振替コマが残る）」も検出しない
    （二重取りは**欠席解除時の確認モーダル**で操作時点に防ぐ方針）／
    休日設定・未消化一覧の×・丸ごと振替の移送で消えたコマの合図は**未消化振替パネルに一本化**する
    （日程表には出さない）。
  - **移行の段取り**：Step 1＝算出関数のみ追加（表示不変・完了）／Step 2＝**開発用教室のみ**有効化／
    Step 3＝staging 実機 → 全教室／Step 4＝旧方式（テンプレ由来の生成・payload・通常側の表示調整）を撤去。
  - ⚠️ **Step 2 の最優先チェック**：新方式では増コマも盤面から直接数えるため、
    `buildSerializedScheduleCountAdjustments` の**増コマ +1 を同時に外さないと 1 つの増コマで予定数が 2 増える**。
    v1.5.468 の二重減算とまったく同じ「新経路を足して旧経路を外し忘れる」型。
  - ⚠️ **移行時に一度だけ過去月の数字が動く**（既存の通常側 `scheduleCountAdjustments` を読み捨てるため）。
    **消さずに読み捨てる**こと（消すとロールバック時に旧版の数字が復元できない）。
  - 旧方式の経緯（Step 4 完了までは有効）：`7ac998d`（v1.5.50「contract-based」）の方針は
    `cc5e5e8`（2026-05-29 の寄せ集めコミット）で大半が無言で巻き戻り、増コマ +1 は 2026-08-05 の
    オーナー確定で意図的に上書きした差分。詳細は `src/utils/scheduleHtml.ts` の該当コメント。
- **担保状況：○**（回帰テストで固定済み。**この同一性補完を spread 単純化で消すと再発**する＝重複ゼロ原則の実例）。
- **two-strike 到達したが昇格は見送り（オーナー判断 2026-08-05）**：違反 2 件目が確定したため §2 の
  昇格検討にかかったが、**今回は準観察のまま据え置く**とオーナーが判断した。
  **代わりの担保**＝上記「マトリクステストファイル」節の経路テスト群（希望数の正本の排他／削除後の希望数を
  実数値で固定／予定側 +1 の対称性）。これらは mutation（排他を外す・片走査に戻す・種別ガードを戻す）で
  落ちることを実測確認済みで、マトリクス相当の網羅を経路テスト側で持つ。
  **次に 3 件目の違反が出たら再度昇格を検討する。**
- **未修正の隣接疑い（兄弟監査 2026-08-05・要オーナー判断）**：**欠席にした在庫由来講習がある日を「その日の
  生徒を全コマ削除」すると、在庫へは +1 返却済みなのに希望数が −1 される**（`reconcileHolidayDeskStockReturns`
  が absent/moved を「会計済み」として `returnedEntryIds` に入れないため、`disposeDayDeskEntries` が
  希望数 −1 を積む）。在庫と希望数が食い違う同型の疑い。v1.5.468 では**挙動を変えていない**（スコープ外）。

---

### INV-06【強制】在庫の実態一致

**保証文**：未消化の振替・講習在庫は盤面実配置・提出の実態と一致し、明示操作なしに増減しない。

- **層**：強制。
- **明示操作の定義**：配置・登録による**消化＝正**。リロード／ロールバック／テンプレ上書き／凍結境界による
  増減は**違反**。誤増（消化済みの再出現）も違反。
- **違反履歴**（transcribe）：
  - makeup-stock-miscount 群（残数が実態と食い違う）：
    - `a12ee32` / v1.5.65 … stale closure 二重減算。
    - `6829dd1` / v1.5.78 … `consumeOriginDates` 不一致フォールバック（`remaining.shift()` が無関係 origin を誤消費）。
    - `a9e9cc7` / v1.5.85 … 振替出席/振無休で再カウント（`statusSlots` へ退避した消化済みを見落とし）。
    - `0f6cb14` / v1.5.69 … 手動調整へ誤変換。
    - `5b13724` … stale 漏出／undo 復元せず。
    - `93cf1f9` / 2026-06-11 … 1 配置で 2 減算（`overAssignedRegularLessons` の二重減算）。
    - `cbc68c0` / 2026-05-29 … クォータ算出。
    - `8cf996c` / 2026-06-25 … 選択でなく最古日付に割当。
    - `2d73401` / 2026-04-15 … テンプレ上書きで消化済み再出現。
    - `cbdf308` / 2026-04-10 … 同一講師複数机で誤ストック生成。
  - occupied-origin-suppressed-makeup-loss（本番 7/20 振替 4 名消失）：
    - **v1.5.363** / 2026-07-03 … `computeOccupiedSlotOrigins`（`moved_` の手動移動授業を占有と誤認し偽 origin を
      大量生成）経路を廃止。その一括削除（`suppressedMakeupOrigins`）が本物の 7/20（海の日）休講日振替も巻き込み、
      緑が丘 白川(数)/古賀(英)・日大前 劉(数)/神(理)の 4 名分が未消化一覧から消失。
    - v1.5.366 → **v1.5.367** … 一時復帰ボタンで手動復帰後に撤去。
    - **Issue #39**（2026-07-11 時点 **OPEN**）… 恒久復帰 UI は未実装。オーナー確定で「凍結前は自動計上対象外・
      手動追加が正」を仕様化（`buildMakeupStockEntries` は凍結後 `regularLessons` のみ参照し履歴合成しない）。
  - lecture-stock-clear-status-wipe（講習の欠席解除で消化記録が丸ごと消える／本番 緑が丘 犬飼凜 数4回の二重計上）：
    - **v1.5.447** / 2026-07-17 … `handleClearStudentStatus`（欠席解除）の session 講習相殺が
      `removeLectureStockCount`（結果 0 以下でキーごと削除）を負値デルタ台帳 `manualLectureStockCounts`
      （負=消化を記録）へ誤用。負値状態で欠席解除すると消化記録ごと消え、盤面配置済みでも未消化へ再出現
      （誤増＝消化済みの再出現）。実データは 15 分毎バックアップで -4→(欠席×2)-2→(解除×2)キー削除=0 を確認。
      修正＝欠席解除は盤面へ再配置し直す操作なので `reconsumeSessionLectureStock`（`appendLectureStockCount(-1)`・
      負値保持）で 1 回積み直す。`handleMarkStudentAbsent` の戻し(+1)と対称。
    - ⚠️ **テンプレ上書き（`handleSaveRegularLessonTemplate` 分岐C）へは同関数を統一しない**（INV 監査 2026-07-17）。
      あちらは範囲一掃＋「盤面配置+未消化=提出希望数」の均衡復元（`8436bf5`）で意味が異なり、-1 純減は逆に
      未消化を過少計上する。freeze 境界絡みの過少/過剰は **Issue #48**（OPEN・要仕様確定）。
  - holiday-toggle-status-double-count（「その日を休日に設定」の statusSlots 二重計上／誤返却・Issue #49）：
    - **v1.5.448** / 2026-07-18 … `handleToggleHolidayDate` の statusSlots ループが出欠種別（`status`）を見ず
      出欠済みの講習/振替を無条件に在庫へ +1/振替 origin していた。欠席（既に mark-absent が戻し済み）で二重計上、
      移動（消化は移動先が保持）でも誤増。修正＝在庫戻しを **status 認識**にし、机1つ分を純関数
      `reconcileHolidayDeskStockReturns` へ切り出し。`HOLIDAY_STOCK_RETURNABLE_STATUSES={attended, absent-no-makeup}`
      だけ戻す（mark 時に在庫未処理＝消化 -1 のまま）、`absent`/`moved` は触らない（会計済み）。studentSlots は必ず戻す。
    - ⚠️ **回帰の教訓（同 push の INV 監査で是正）**：当初「statusSlots は一律で触らない」としたが、これは
      `commit 0260430` の attended/振無休 対応の巻き戻しで、**過少計上（消化 -1 の孤児化）を新規導入**していた。
      休日化の在庫会計は各 `handleMarkStudent*` の会計と 1:1 対応（absent=+1済み／attended・振無休=未処理）で決まる。
      **一律スキップも一律返却も誤り**＝status 認識が必須。ハンドラ側の会計を変えたら本セットも見直す。
    - 併せて講習在庫キーを `resolveLectureStockStudentKey`（`managedStudentId` 最優先→名簿逆引き→`name:`）へ統一。
      配置(-1)は提出 studentId で消化し配置コマの managedStudentId にその id を焼くため、戻し側が名前逆引きだけだと
      **改名**でキーがズレる。戻し 6+1 経路（欠席/欠席解除/格納/削除/休日化/テンプレ上書き）に適用（非改名は挙動不変）。
  - moved-makeup-absence-loss（別日へ移動した授業を休みにすると未消化振替から消滅／緑が丘 室長報告 2026-07-31）：
    - **v1.5.459** / 2026-07-31 … 通常授業を別日へ**移動**した振替コマは台帳（自動休校日 / 同時間帯重複 /
      手動調整）に origin を登録しない。「盤面に置かれていること」＋「消化(`plannedMakeups`)と使用済み origin が
      打ち消し合う」均衡だけで残 0 を保つ設計のため、この振替コマを**休み**にすると盤面から消え、
      `absent` は消化に数えない（正）のに戻すべき origin が台帳に無く、**未消化振替へ1件も戻らず授業が消滅**していた。
      在庫由来（台帳に origin あり）の振替コマは正しく戻るため、**同じ「休み」でも生徒によって結果が違う**
      ＝報告「休みにしても未消化振替に入らない生徒がいる」の正体。誤減（実施されなかった授業の消滅）側の INV-06 違反。
    - 修正＝`makeupStock.ts` の `collectAbsentMakeupOrigins` が、`absent` の出欠記録が盤面にある限り
      その振替元日を origin として**算出で復元**する（台帳へ書き戻さない）。`resolveEffectiveMakeupOriginDates` を
      有効 origin 決定の唯一の権威関数にして 4 発生源（自動休校日/重複/手動/振替コマ欠席）を統合。
    - ⚠️ **台帳へ materialize しない理由（回帰防止）**：`handleMarkStudentAbsent` で origin を積むと
      (1) 在庫由来の振替では「積む＋台帳 origin の再浮上」で二重計上、(2) 休み解除側の同条件ガードと非対称になり
      解除後に origin が残る、(3) **既に壊れて保存済みのデータが復旧しない**。算出方式なら休み解除で自動的に消え、
      読み込み直しただけで過去分も復旧する。`statusSlots` を持つ週は `trimBoardWeeksForMemory` が必ず保持するため
      算出元は失われない。ハンドラ側の非対称ガードは意図的なので条件を緩めないこと。
    - 兄弟監査で同根の穴を1つ是正：**格納（未消化振替へ戻す・`handleStoreStudent`）** も同じガードで
      移動しただけの振替コマを origin なしで盤面から外していた（出欠記録すら残らないため算出でも救えない）。
      `resolveStoreMakeupOriginDate` で「台帳に origin があれば積まない（二重計上防止）／無ければ振替元日で積む
      （消滅防止）」に是正。削除（`handleDeleteStudent`＝`suppressedMakeupOrigins` へ退避）・振無休・出席・移動は
      仕様どおり未消化にしないことをマトリクスで固定。
  - deleted-then-absent（削除した日を後から休みにしても未消化振替に入らない／緑が丘 室長報告 2026-07-31・大槻 8/5）：
    - 上の moved-makeup-absence-loss と**別原因**。`suppressedMakeupOrigins`（削除＝×／コマ削除で積まれる）は
      **生徒×科目×日付**の永続フィルタで、解除するUIが無い（Issue #39）。そのため「削除 → コマを足し直す →
      あらためて休み」をしても、その日付は恒久的にブロックされ**未消化振替へ入らなかった**。過去に削除した日を
      持つ生徒だけ症状が出るため「同じ休みでも生徒によって違う」に見える。
    - 対処＝**順序方式（後にやった操作が勝つ）**。「休み」「格納」は `clearMakeupOrigins` でその元コマ日の抑制を
      **解除**し、削除は従来どおり抑制を積む。詳細と非対称（休み解除では抑制を戻さない）は
      `docs/spec-makeup-stock.md` §2 削除（抑制）。
    - ⚠️ **算出で一律に無効化する方式は採らない**（オーナー確定 2026-07-31）：「休みの記録がある日は抑制を無視」に
      すると、**意図的に×で消した分まで復活する**（本番実データで緑が丘 13 件・日大前 4 件が復活し、うち 10 件は
      元の予定コマが盤面に残ったまま行だけ消した＝意図的な削除だった）。順序方式なら過去データは動かない。
    - 併せて **B-3（手動追加コマの休み）** を確定：手動追加した通常・振替・増コマの休みも未消化振替へ戻す
      （日程表の実績カウントが `manualAdded` を除外しないため、戻さないと1コマ消える）。
      `collectAbsentMakeupOrigins` の `manualAdded` 除外を撤廃。消化（配置）側の非対称は据え置き＝在庫の純増は許容。
  - same-day-two-lessons（同じ日に同じ科目が2コマある日、両方休みにしても1コマしか未消化にならない）：
    - origin の同一性が**日付**単位だったため、同日同科目の2コマを両方休みにしても残1（1コマ消える誤減）。
      対処＝**origin を「日付＋時限」トークン（`YYYY-MM-DD#限`）で識別**する。詳細は
      `docs/spec-makeup-stock.md`「origin の同一性は『日付＋時限』」。
    - ⚠️ **時限不明の origin はワイルドカード**（同じ日付の時限つき origin と同一視）。これを別物として数えると
      「元の授業の休み」と「その振替コマの休み」＝同じ1コマが2件になり誤増する。**過大計上しない側に倒す。**
    - 抑制・解除・消化・一覧選択も同時に時限単位へ揃える（片方だけだと穴が空く）。時限なしの抑制（旧データ）は
      その日付を丸ごと落とす後方互換を維持し、消化は完全一致→同日フォールバックの順で突き合わせる
      （フォールバックが無いと旧データの配置で消化済みが再出現する）。
  - derived-origin-downstream-loss（休みにした振替コマの記録を**破棄する操作**で在庫ごと消える／2026-08-01 下流監査）：
    - 上の moved-makeup-absence-loss を**算出方式**（台帳へ書き戻さず、absent の出欠記録が盤面にある限り
      振替元日を復元する）で直した結果、その在庫は「出欠記録が盤面に残っている間だけ成立する仮の姿」になった。
      **記録を消す下流操作が確定させないと、在庫ごと消える**。実測で 2 経路が消滅していた（読み取り専用調査）：
      ① 休みにした日を後から**休日設定**（`handleToggleHolidayDate` が `desk.statusSlots = undefined` で破棄。
      `HOLIDAY_STOCK_RETURNABLE_STATUSES` が `absent` を外している前提＝「mark-absent が積み済み」は
      **在庫由来・通常・講習にしか当てはまらない**）。② その日が**非営業日**になる（`collectAbsentMakeupOrigins` が
      `isOpenDay=false` のセルを飛ばしていた）。
    - 対処：①**破棄する側で台帳へ確定**（`resolveMakeupStatusOriginToMaterialize`＝台帳に同じ日付の origin が
      あれば積まない／無ければ振替元日で積む）。②`collectAbsentMakeupOrigins` は
      **非営業日セルも走査**する（在庫の根拠を集めるので、消化を数えない他の収集関数とは判定が違う）。
    - ⚠️ 台帳の有無を判定する一覧は **算出由来（absent）を除いた版**（`collectMakeupOriginDatesByKey` の
      `includeAbsentMakeupOrigins: false`）を使う。含めると**算出で復元した自分自身を「台帳にある」と誤読**して
      確定を取りやめ、記録の破棄と同時に消える。
    - ⚠️ 同じ元コマを指す absent 記録が複数あっても台帳へは**1 件だけ**確定する（算出側はトークン集合で畳むため、
      そのまま写すと誤増する）。
    - 併せて `resolveStoreMakeupOriginDate`（格納）の**台帳突き合わせを日付単位へ是正**：呼び出し側が渡す台帳は
      時限つきトークン（`YYYY-MM-DD#限`）なのに素の `includes` で**日付だけ**と比べており、在庫由来の振替を
      「台帳に無い」と誤判定して二重に積む誤増があった（時限単位化 `d02379c` の取りこぼし。ユニットの fixture が
      トークン化前の形のままだったため緑をすり抜けていた）。
    - **教訓**：隣接操作（休み／休み解除／振無休／出席／格納）の**兄弟監査だけでは足りない**。算出で状態を
      復元する設計にしたら、**その状態を消す下流操作**（休日化・全消し・テンプレ上書き・非営業日化）を必ず列挙する。
      v1.5.448 が同じ関数に残した警句「ハンドラ側の会計を変えたらここも見直す」が的中した形。
      ※**丸ごと振替**（2026-08-02 新設・Issue #40）は出欠記録が両日にあれば**事前ブロック**するため、この下流列挙に
      **該当しない**。将来ブロックを緩めるならこの列挙へ追加し、2 由来 × 出欠状態の突き合わせを必ず行うこと。
  - makeup-origin-source-asymmetry（振替コマの**由来**で結果が食い違う／2026-08-02 オーナー指摘の対称性監査）：
    - 振替コマの**在庫由来**（台帳に origin あり）と**移動由来**（在庫を経由していない）で、盤面から外す操作の
      結果がズレていないかを実測で総当たりした（未出欠の配置／出席済み／休み／振無休 × 2 由来）。
      **出席済み・振無休で非対称が実在**：休日設定すると 在庫由来=残2／移動由来=残1（移動由来が1コマ消える）。
    - 真因：出欠記録を在庫へ返すとき**当日**（記録が載っているコマの日）を元コマとして積んでいた。在庫由来は
      台帳 origin の再浮上と合わせて**二重計上**、移動由来は当日 origin が同じ日の別 origin に**吸収されて消える**。
    - 対処：**振替コマの出欠記録は状態を問わず「振替元日」で会計**し、台帳にあれば積まない
      （`resolveMakeupStatusOriginToMaterialize` へ一本化。`moved` は会計を移動先が持つので対象外／手動追加は
      在庫未消費のため返さず、`absent` だけ §B-3 の確定により例外）。8 パターンすべてで由来による差が消えたことを実測。
    - **ルール**：内部の道筋（再浮上に任せる／台帳へ確定する）は違ってよいが、**ユーザーから見た残数は必ず一致**させる。
      新しく「盤面からコマを外す操作」を足すときは、必ず 2 由来 × 出欠状態で突き合わせる。
  - clear-day-stock-return（「その日の生徒を全コマ削除」の在庫会計／2026-08-02 オーナー確定）：
    - 従来は「ストックへの移行は行いません」と明示しつつ、**台帳に origin がある在庫由来だけが勝手に戻る**非対称。
    - 確定仕様：**在庫から出したコマ（振替・ストック由来の講習）は未消化へ返す／通常授業・体験・増コマ・手動追加は
      返さず希望回数 −1**。会計は休日設定と同じ `reconcileHolidayDeskStockReturns` に委譲し、違いは
      `includeRegularLessons:false` だけ。⚠️「返す」と「希望回数 −1」を同時にやらない（`returnedEntryIds` で伝える）。
    - 原則：**教室都合で外す（休日設定・全コマ削除）＝返す／1コマずつ「要らない」と消す（削除・一覧の×）＝返さない**。
  - move-displaces-absent-record（生徒移動が移動先の absent 記録を破棄し「休」表示と算出在庫が消える／
    Issue #57・**v1.5.481** / 2026-08-29・第三者手動テスト No.113 で発見）：
    - `computeStudentMove` が移動先（と入れ替え着地）スロットの出欠記録を**一律 null 化**していた
      （`ee5728c`「移)日付の滞留を消す」の実装が moved 以外まで巻き込み）。移動由来振替の absent は
      台帳に origin が無く**算出で復元**される（moved-makeup-absence-loss の方式）ため、記録が消えると
      未消化振替が 1 件無言で消滅（誤減）。derived-origin-downstream-loss の教訓「statusSlots を消す経路を
      列挙する」から**生徒移動が漏れていた**。
    - 対処：①クリアは **moved マーカー限定**にし absent／振無休は保持（「休」表示と算出根拠が残る）
      ②上書きが避けられない箇所（moved マーカー書き込み・出欠付与 3 ハンドラ）は、破棄前確定の判定を
      共通関数 `materializeDisplacedStatusEntryIntoLedgers` へ一本化して確定（`reconcileHolidayDeskStockReturns`
      も同関数へ委譲・挙動不変）。moved マーカー書き込みは消える記録を `displacedStatusEntries` で
      呼び出し側（盤面移動／日程表D&D）へ返し、呼び出し側が確定してから commit する。
    - ⚠️ moved マーカーの削除まで保持に変えると `ee5728c`（移動日付の引き継ぎ）が再発する。
      ⚠️ 確定の台帳判定は `ledgerMakeupOriginDatesByKey`（`includeAbsentMakeupOrigins: false`）を渡すこと
      （算出で復元した自分自身を「台帳にある」と誤読すると確定が取りやめられ同時消滅する）。
  - clear-day-then-holiday-restack（全コマ削除した日の休日設定で処分済み通常授業の振替が積み直される／
    Issue #58・**v1.5.481** / 2026-08-29・第三者手動テスト No.121「5週目対応が難しい」で発見）：
    - 全コマ削除は通常授業を「返さず希望回数 −1（もうやらない）」で処分する（clear-day-stock-return）のに
      振替抑制（`suppressedMakeupOrigins`）を積まない非対称があり、休日振替の自動計算
      （`computeAutomaticShortageOrigins`）が**盤面ではなくテンプレを根拠に**発火するため、
      同じ 1 コマに「希望 −1」と「振替 +1」が両方かかっていた（誤増）。
    - 対処：単発削除（`handleDeleteStudent`）と同様に抑制を積む（新設 `collectClearedDayMakeupSuppressions`・
      処分で lesson/statusSlots を破棄する**前**に収集）。
    - ⚠️ **時限つき（テンプレ上の元時限）で積む**：時限なしの日付ワイルドカードだと同じ生徒×科目の
      欠席済み origin（台帳確定済みの在庫）まで巻き込んで消す。同日移動した授業は盤面の時限がテンプレと
      ズレるため `sameDayMoveSourceLabel` の元時限で積む（現在の時限だと自動 origin に当たらず取りこぼす）。
    - ⚠️ **absent／moved の記録には積まない**：absent は mark-absent が台帳確定済み（抑制すると立っている
      在庫が消える）。moved の会計は**移動先の振替コマ**が持ち、「移動先を休み → 移動元日を全コマ削除」の順で
      算出復元 origin を抑制が消す（INV 監査 2026-08-29 で実証・端到端テストで固定）。attended／振無休は積む。
    - 「全コマ削除 → コマを足し直して休み」は deleted-then-absent の順序方式（`clearMakeupOrigins`）が
      そのまま効いて振替に入る。※**丸ごと振替の振替先処分には抑制を積んでいない**（意図的な差・
      フォローアップ Issue で裁定予定。統一リファクタで安易に揃えないこと）。
- **マトリクステストファイル**：`inv06-makeup-absence-stock.matrix.test.ts`（2026-08-29・**56 テスト**。
  Issue #57 の下流列 1 件〔欠席記録のある席へ生徒を移動しても未消化振替が消えない〕と Issue #58 の
  下流列 6 件〔全コマ削除の抑制で休日設定の積み直しを防ぐ（時限つき）／absent はスキップし在庫を守る／
  attended・振無休は積む／moved はスキップ（積むと移動先休みの算出 origin が消える誤減・端到端で固定）／
  同日移動はテンプレ元時限で積む／順序方式で足し直し→休みが入る〕を追加。以下は 2026-08-02 時点の 49 テスト。由来の対称性 3 件
  〔出席済み／振無休の休日設定で在庫由来と移動由来の残が一致・`moved` は触らない〕と「その日の生徒を全コマ削除」列 7 件
  〔在庫由来と移動由来がどちらも残1／出席済みも一致／通常は返さない／体験・増コマ・手動追加は返さない／
  ストック由来の講習は返し手動追加の講習は返さない／`returnedEntryIds`〕を追加。旧 `it.todo`〔全コマ削除の裁定〕は
  2026-08-02 のオーナー確定で上記 7 件へ置換済み。以下は 2026-08-01 の下流列 6 件：
  休日設定で移動振替／手動追加が台帳へ確定・在庫由来と通常は触らない〔誤増しない〕・同じ元コマ2件で1件だけ確定・
  非営業日化でも残る・台帳突き合わせは時限つきトークンでも日付で当てる。以下は
  2026-07-31 時点の 27 テスト：
  休み × 通常/同日移動/在庫由来の振替/移動しただけの振替/手動追加、休み解除の往復、隣接出欠〔振無休・出席・移動〕の
  誤増なし、格納の origin 判定、個別非表示化した元コマを復活させないこと、**削除→足し直し→休みで入ること
  （順序方式）**と `clearMakeupOrigins` の単体挙動、**同日2コマの時限単位**〔両方休みで残2／同じ1コマを指す
  origin は残1／時限不明の吸収／時限つき抑制は別コマを巻き込まない／時限なし抑制の後方互換／解除の時限指定／
  配置選択の引き継ぎ〕）＋`inv06-lecture-stock-reconciliation.matrix.test.ts`（2026-07-17・4 テスト：
  犬飼シナリオ再現／欠席化⇔欠席解除の往復対称性／負値保持）＋`inv06-holiday-stock-reconciliation.matrix.test.ts`
  （2026-07-18・16 テスト：休日化を全 status〔absent/moved=スキップ・attended/absent-no-makeup=戻す〕×session講習/通常で
  両方向固定／振替 origin の dateKey 区別／同一 index 併存／改名キーの正準化）
  ＋`inv06-whole-day-transfer.matrix.test.ts`（2026-08-02・**35 テスト**・Issue #40「丸ごと振替」。
  2026-08-03 に テンプレ足場講師の日単位抑止 3 件〔両日で湧かない／**テンプレに授業がある日（本番形＝抑止で lesson が
  消えた管理机の講師も落ちる。strip と抑止の順序入替を検出する）**／抑止していない日は従来どおり付く〕と
  共通処分関数 `disposeDayDeskEntries` 4 件〔丸ごと振替モード=メモ消去+抑止キー／全コマ削除モード=メモ保持+抑止なし／
  `statusSlots` も同じ規則で処分／返却分は希望回数−1をスキップ〕を追加。以下は 2026-08-02 時点の 29 件：
  振替先処分×全種別〔通常=台帳不変+回数−1+暗黙再出現なし／体験・メモ=消去のみ／**増コマ=消去のみ（オーナー指示）**／
  session講習=+1・手動講習=返さない／振替の2由来対称〔§B-2-2・処分側〕／手動追加振替=返さない／返却分は回数−1なし〕、
  移送側の在庫中立〔全台帳 deep-equal 不変・通常→振替変換・元日復帰・講習/増コマ/体験素通し（2人目スロット保持）・
  移送側の2由来対称・空 lesson シェル非伝播・managed id 振り直し〕、**複数コマ×複数机の1対1マッピング**、
  **主用途の連鎖（丸ごと振替→振替元を休日設定・過去日=自動休校日 origin 発火構成で残数中立）**、
  講師帰属と再マージ耐性〔manual固定+ID補完・削除tombstone・overlay往復で復活しない・消える講師人数の開示〕、
  ブロック系〔出欠記録・moved マーカー・空日・同日・範囲外・コマ構成不一致〕。
  mutation 3 件〔通常も返す／移送側で台帳に積む／机 index 取り違え〕で落ちることを確認）。
  加えて `makeupStock.test.ts`（3→2 で 1 件だけ減る／出席消化と欠席未消化の区別／
  suppressed／マイナス残廃止）・`makeupStockSnapshot.test.ts`・`lecturePendingItems.test.ts` で担保。
  振替側・テンプレ上書き複合（配置+欠席→上書き）のマトリクス化は未了（Issue #48 と併せ次の違反時に拡張）。
- **担保状況：○（-）**（残数計算・消化アルゴリズムは固定済み・休日化は status 認識＋下流確定で固定。
  **「`studentSlots` と `statusSlots` の両方を走査する」ルールが属人的**で再発温床（休日化はマトリクス化で緩和）。
  全消し（`handleClearStudentsOnDate`）の裁定は 2026-08-02 オーナー確定でマトリクス化済み。丸ごと振替も同裁定へ
  委譲しマトリクス化済み。復帰 UI（Issue #39）・テンプレ上書き複合の均衡（Issue #48）は未担保。
  **算出で復元する在庫（absent 由来）は「盤面に出欠記録がある間だけ」成立する**ため、statusSlots を消す経路を
  追加・変更するときは必ず確定（materialize）の要否を判断する。既知の消す経路＝休日設定／全コマ削除／
  生徒移動・入れ替え（v1.5.481 で moved 限定クリア＋確定に対応）／出欠付与の上書き（同・確定に対応）。
  確定判定は `materializeDisplacedStatusEntryIntoLedgers` に一本化済み＝新しい経路もこの関数を通すこと）。

---

### INV-07【強制】提出データの確定不変

**保証文**：QR 提出とその反映（残数／配置／出席者一覧）も登録解除も、確定した状態はアプリ都合の処理
（復元／ロールバック／保存競合／リロード／購読レース）で変わらない。提出を消せるのは**ユーザーの明示的な
登録解除のみ**。

- **層**：強制。
- **確定仕様**：復元／ロールバックで提出（`lectureSubmissions` 独立コレクション）は**残る**のが正
  （復元/ロールバックの対象外）。逆に、**解除したのに古い購読で復活する**（v1.5.392）のも違反（双方向）。
- **違反履歴**（transcribe）：
  - parent-owned-submission-fields-loss（集団参加・保護者所有フィールドの一括消失）：
    - `86f68ad` / **v1.5.318** / 2026-06-24 … 生徒日程表の登録（`schedule-student-count-save`）に
      `groupClassParticipation` を載せたが App.tsx 側ハンドラ未更新でメッセージ無視。
    - `98af2c1` / **v1.5.328** / 2026-06-26 … `onSubmitted` が `if(existing?.countSubmitted) return` で提出全体を
      スキップし保護者所有フィールド更新も握りつぶす。
    - **v1.5.335** / 2026-06-27 … 室長手動登録の書き戻し漏れ。
    - **v1.5.336** / 2026-06-27 … v1.5.328 の修正が空 doc で既存を上書き（本番データ消失。union＝追加のみ方式で恒久修正）。
    - 影響：中 3 の集団理科/社会が本番で一括消失（緑が丘 5 名・日大前 2 名）、岩佐 s085 の optionChecks が空に。
  - qr-teacher-placement-not-reflected（講師 QR 反映）：
    - `ab013ee` / **v1.5.356** / 2026-06-30 … QR 一括取り込みが講師ごとに単一 useState の
      `setTeacherAutoAssignRequest` をループ発行し最後の 1 人で上書き（items[] 化で修正）。
    - `eba52cb` / **v1.5.357** / 2026-06-30 … `countSubmitted` は自動同期で永続化だが盤面配置は手動保存のみの
      非対称。保存前リロードで配置だけ揮発しスタック（起動時 `reconcileSubmittedTeacherPlacements` で自己修復）。
  - lecture-submission-reset-race：
    - `9e26d7d` / **v1.5.392** / 2026-07-04（監査領域 7 B4）… `resetLectureSubmissionDoc` の getDoc→setDoc の間に
      古い `onSnapshot` が割り込み、解除した提出（`countSubmitted`）が復活。ガード ref への `.add` 箇所が
      src 全体に無く常に空集合で全通過していた（`recentlyResetGuard` で修正）。
- **マトリクステストファイル**：**未整備（義務・two-strike 済み）**。現状は `submissionReflection.test.ts`
  （空 `{}` は既存を消さない）・`recentlyResetGuard.test.ts`（TTL 経過までブロック）・`specialSessionModel.test.ts`・
  `lectureSubmission.test.ts`・`ScheduleBoardScreen.test.ts`（reconcile）で暫定担保。**次の違反時にマトリクス化**する。
- **担保状況：○**（「消してはならないガード」を正本
  （`spec-special-session-submission.md`）に明記し、双方向（消失・誤復活）を固定済み）。

---

### INV-08【強制】教室分離

**保証文**：ある教室の操作・データは他教室に影響しない。

- **層**：強制。
- **担保は二層**：(a) **コードテスト**（`resolveWorkspaceSyncTargetClassrooms` 等）＋
  (b) **CLAUDE.md の本番データ保護運用ルール**（書き込み検証は開発用教室 `v8OZ7zH8vONNHjjYVcR1` のみ）。
  **two-strike の判定はコード起因分のみ**で数える（運用起点の 2026-06-06 / 06-13 は別枠）。
- **違反履歴**（transcribe）：
  - classroom-cross-contamination（教室越え上書き）：
    - 2026-06-06 … Claude 自動チェックが `actingClassroomId` 未制御で復元/コピー（**運用起点**・別枠。
      日大前のスナップショットが緑が丘データで上書き）。
    - `952e562` / **v1.5.297** / 2026-06-11 … 浅いコピーの参照共有（sanitize が spread のみで配列を
      ディープクローンせず）。
    - `e549868` / **v1.5.299** / 2026-06-11 … `targetClassroomIds` 未指定＝全教室フォールバック（増幅器）。
    - `8316830` / **v1.5.300** / 2026-06-11 … `undoSnapshot` 残存。
    - 2026-06-13 … undoSnapshot 系統の再発（**運用起点**・別枠）。
    - `e4a882a` / **v1.5.313** / 2026-06-21 … `applyWorkspaceSnapshot` が編集 state 未読込で desync 誤書込。
  - board-share-token-cross-classroom：
    - `7d17ef1` / **v1.5.269** / 2026-06-04 … `classroomSettings.boardShareToken` がバックアップ復元/教室コピーで
      別教室へ複製され、複数教室が同一トークンで `boardShares/{token}` を上書き衝突（後公開が勝つ）。
  - dev-classroom-qr-token-contamination：
    - `b2e2048` / **v1.5.415** / 2026-07-09 … 開発用は本番の生データをコピーするためコピー元の提出トークンが残存し、
      日程表 QR をスキャンでトークンの `classroomId`＝本番へ書込（西岡/大塚/山崎ら本番生徒が誤提出）。dev 限定の
      発行元教室タグ＋ガードで恒久対策。
    - `aeb1d97` / **v1.5.416** / 2026-07-09 … ガード副作用で開発用 QR 非表示の回帰（未タグの自教室トークンまで
      除去。再発行で解消）。
- **マトリクステストファイル**：**未整備（義務・two-strike 済み）**。現状は `App.test.ts`
  （`resolveWorkspaceSyncTargetClassrooms`＝未指定なら操作教室のみ／2026-06-21 回帰＝出所一致教室のみ反映／
  コピー先と元が参照非共有）・`developmentClassroom.test.ts`（自教室発行タグ一致時だけ信用）・
  `scheduleHtml.test.ts` で暫定担保。**次の違反時にマトリクス化**する。
- **担保状況：○（二層）**（コードテスト＋運用ルールの二層で守る。2026-06-06 / 06-13 の運用起点はコード非対象で、
  CLAUDE.md 本番データ保護ルールが担保）。

---

### INV-09【強制】警告・割振対象判定の実態一致

**保証文**：盤面の警告および割振対象判定は、保存済み設定でなく**実態（盤面・提出）**と一致する。

- **層**：強制。
- **違反履歴**（transcribe）：
  - regular-lesson-false-warning（固定通常授業への誤警告）：
    - `248427` / **v1.5.293** / 2026-06-10 … 優先事項区分の一律ハイライト（警告判定が制約/優先を区別せず）。
    - `a2381c4` / 2026-06-15 … 「通常講師のみ」ルール。
    - `5989ccc` / **v1.5.342** / 2026-06-28 … 「指定時限禁止」ルール。
    - **v1.5.394** / 2026-07-04 … 「ペア制約」ルール。
    - 構造：各ソフト制約が `lessonType==='regular'` を割振り対象外として除外しておらず、1 ルール直す度に別ルールで
      同型再発（`shouldWarn*` / `resolvePairConstraintWarningSeverity` に純関数化）。
  - regular-teachers-only-array-source：
    - v1.5.315 … 1 回目（曜日別→any-day・配列参照のまま不十分）。
    - `c04794e` / **v1.5.317** / 2026-06-22 … 「通常講師のみ」判定が静的 `regularLessons` 配列頼みで盤面と食い違い、
      基本データ編集後に許可集合が空になる。盤面の通常授業から担当講師を集めて和集合で判定
      （`collectStudentRegularTeacherIdsFromWeeks`）に修正。ソフトなので 0 件でなく別講師へ配置として顕在化。
- **マトリクステストファイル**：**未整備（義務・two-strike 済み）**。現状は `ScheduleBoardScreen.test.ts`
  （通常授業は許可講師集合に無くても違反にしない／禁止時限対象外／ペア制約対象外、在庫配置は従来通り違反／
  担当講師集合は全曜日の通常授業から集約・在籍期間外除外）で暫定担保。**次の違反時にマトリクス化**する。
- **担保状況：○**（警告判定を純関数へ切り出し、制約/優先の区別と割振り対象外(通常授業)の扱いを固定済み）。

---

### INV-10【準観察】テンプレ反映日以前の不変

**保証文**：テンプレ反映日以前のコマ表はテンプレ編集で変わらない。

- **層**：準観察（非強制）。**two-strike で昇格**。（反映境界週の欠落＝逆方向は INV-04/05 側で拾う。）
- **実装**：`templateFreezeBeforeDate`（v1.5.3）。
- **違反履歴**（transcribe）：
  - `6937a15` / **v1.5.3** … `templateFreezeBeforeDate` 導入（freeze 日以前のセルをオーバーレイから除外する
    仕組みの整備）。
  - `fbe2246` / 2026-04-15 … テンプレ初回反映週で授業欠落（反映日の境界処理でセルが正しく合成されず。
    ＝逆方向は INV-04/05 側）。
- **マトリクステストファイル**：準観察のため未整備。経路テスト `regularLessonTemplate.test.ts`
  （有効開始日在籍者のみ・有効年度のみ生成）・`ScheduleBoardScreen.test.ts`
  （`filterTemplateOverwriteHolidayDates`＝反映日以降の手動休校日を破棄）で担保。
- **担保状況：○**（凍結境界の両方向を経路テストで固定済み）。

---

### INV-11【準観察】選択対象の忠実配置

**保証文**：手動配置は選択した対象（科目／生徒／講師）をそのまま配置する。

- **層**：準観察（非強制）。**two-strike で昇格**。
- **違反履歴**（transcribe）：
  - lecture-subject-selection-ignored：既存バグ（2026-03-20 / 06-15 由来）→ `2a8a9e8` / **v1.5.364** / 2026-07-03（修正）…
    未消化講習の手動配置が選択科目を無視し常に先頭科目を配置（中 3 で「数学」を選んでも「英語」が置かれる。
    振替側は `rawKey` / `selectedMakeupStockRawKey` で選択科目を尊重するが、講習側は item を渡さず
    `buildLecturePendingItems(entry)[0]` を無条件配置していた。1 科目生徒では偶然正解のため学年で顕在化が異なる）。
- **マトリクステストファイル**：準観察のため未整備。経路テスト `lectureStockPlacement.test.ts`
  （選択科目＝数を返す・先頭英へ落ちない／sessionId 違い区別／空一覧 null）・`scheduleViewMove.test.ts`
  （講習カード移動で科目維持）で担保。
- **担保状況：○（v1.5.364 経路テストのみ）**（講習側の 1 経路を固定済み。振替×講習は別実装で
  片方の修正が他方に自動適用されない点に注意＝`spec-makeup-stock.md` B8）。

---

## 担保状況サマリ

| INV | 保証（1 行） | 層 | 担保 | 備考 |
|---|---|---|---|---|
| INV-01 | 講師帰属の一意（実配置の机の講師 1 名のみ） | 強制 | ○ | マトリクス新設・14 テスト |
| INV-02 | 手動編集の永続化（自動処理で巻き戻らない） | 強制 | ○ | マトリクス 28。講師帰属境界は仕様確定・ロック済み（2026-07-11） |
| INV-03 | 操作の冪等（再マウント再実行しない） | 強制 | ○ | マトリクス未整備・経路テストで担保・次違反でマトリクス化 |
| INV-04 | 日程表の経路一致 | 準観察 | △ | Issue #42 OPEN（payload 非対称）→ 昇格候補 |
| INV-05 | 回数表示の実配置一致 | 準観察 | ○ | spread 単純化での巻き戻り注意。two-strike 到達(v1.5.468)も**昇格は見送り＝準観察維持**(オーナー判断 2026-08-05)・経路テストで担保 |
| INV-06 | 在庫の実態一致（明示操作以外で増減しない） | 強制 | ○（-） | 講習マトリクス新設(2026-07-17・4テスト)・両走査ルールが属人的・Issue #39 復帰 UI / #48 テンプレ上書き複合 未実装 |
| INV-07 | 提出データの確定不変（双方向） | 強制 | ○ | マトリクス未整備・経路テストで担保・次違反でマトリクス化 |
| INV-08 | 教室分離 | 強制 | ○ | 二層（コードテスト＋運用ルール）。運用起点は別枠 |
| INV-09 | 警告・割振対象判定の実態一致 | 強制 | ○ | マトリクス未整備・経路テストで担保・次違反でマトリクス化 |
| INV-10 | テンプレ反映日以前の不変 | 準観察 | ○ | `templateFreezeBeforeDate`（v1.5.3） |
| INV-11 | 選択対象の忠実配置 | 準観察 | ○ | v1.5.364 経路テストのみ |

> ○＝テストで強制されている／△＝OPEN 課題があり昇格候補／○（-）＝守れているが属人的・未実装課題あり。
> 強制 INV のうち INV-01・INV-02 は専用マトリクス（`*.matrix.test.ts`）を実装済み。
> INV-03・06・07・08・09 はマトリクスが義務だが未整備で、既存経路テストが暫定担保し、次の違反時にマトリクス化する。
