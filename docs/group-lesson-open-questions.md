# 集団授業：オーナー判断が必要な保留事項（最後にまとめて回答）

> 実装は「修正しやすい既定」で先に進め、ここに判断ポイントを集約する。
> 各項目は採用した既定（暫定）と、変えたい場合の差し替えやすさをメモする。

## 一覧

| # | 領域 | 採用した暫定既定 | 備考（差し替えやすさ） |
|---|------|----------------|----------------------|
| Q1 | undo/redo | 集団の変更は undo/redo 非対応（保存・PDFは動く） | 履歴(createHistoryEntry/commitWeeks)への取り込みは別途。回帰リスクのある履歴系のため最後に判断。 |
| Q2 | 室長による参加設定 | 集団の参加/不参加は**現状「生徒のQR提出」経路のみ**実装。室長が日程表から直接設定する経路は未実装。 | 現行の夏期講習は既存トークンに `availableGroupClassSubjects` が無く、生徒QRでは集団欄が出ない＝中3を参加にできない。選択肢：(a) 日程表の希望科目数モーダルに室長用の集団参加トグルを追加（埋め込みJS追加・要回帰注意）、(b) 該当生徒のQRトークンを再発行して生徒に再提出、(c) 集団参加は次回講習以降のみ運用。**現行夏期講習で使うなら(a)が必要。** |
| Q3 | 集団欠席時の生徒日程表示 | 登録済み参加者が欠席の日は「集団(理科)（欠）」を取り消し線で表示。 | 表記/様式は容易に変更可（scheduleHtml の group-slot-absent）。 |
| Q4 | 回数表の集団行 | 集理/集社は中3で「名簿(参加 or 手動追加)」に入る生徒のみ表示。希望=範囲内コマ数/実績=出席数。不一致は既存の講習回数警告スタンプが出る。 | 手動追加のみ(非参加提出)の生徒も名簿に入れば回数に出る。参加提出者だけに限定したい場合は isInGroupRoster を調整。 |

<!-- 以降、Phase 5 で出た判断ポイントを追記する -->
