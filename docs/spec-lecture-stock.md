# 確定仕様：講習・講習ストック（To-Be 正本）

> 全体見直しセッション（2026-06-07）でオーナー確認済みの「目標仕様」。
> 既存 `docs/lecture-edit-flow.md` のうち本書と矛盾する記述（削除の戻し挙動・コマ表での編集可否・希望数の不変原則）は**本書を優先**する。

## 1. 希望数はどこが正か

- 講習の希望数（科目ごとの回数）は **QR／日程表（特別講習データ）で生徒が提出した数が正**。
- 希望数の**増加は QR／日程表の提出だけ**で行う。
- ただし**希望数の減少は盤面操作でも可能**：盤面で対象コマを**「空にする」または生徒クリックメニューの「削除」で希望数が −1** される（後述 §4）。
- 生徒日程表で希望科目数を登録した時点では**自動配置されず、講習ストックに残る**。

## 2. 講習ストックの中身（残数の定義）

- 講習ストック残 ＝ **「希望数 − 盤面に置かれた分」＋「盤面から戻した分」**（＝未配置の残数）。
- 集計単位は **講習セッション × 生徒 × 科目**。
- 対象は **countSubmitted（提出済み）かつ希望科目を登録した生徒のみ**。
  - 「通常のみ（regularOnly）」の生徒は講習ストックなし＝カウントしない。

## 3. 配置（コマ表で置く）

- ストックを選んで**個別割振 → 選んだ空欄へ1コマ配置**。
- **自動割振 → 条件に合う候補へ順次配置**。
- 配置しても**希望数は減らない**。減るのは「未配置残数」。
- 講師日程表で登録した講師は、**対象講習期間の空き机へ自動反映**。

## 4. 戻す・削除（★2操作に役割分担）

- **「ストックする（戻す）」**：盤面から外し、**未配置分としてストックへ戻す**。希望数は変えない。
- **「削除」**：そのコマを盤面から消す。**由来を問わず未配置へは戻さない**。
  - ストック由来の配置を削除した場合は **希望数 −1**（＝もう要らない、の意思表示）。
  - 「空にする」も削除と同じ扱い。
- **コマ表で手動追加した講習はストックへ戻さない**（不要なら削除）。手動追加分は希望数に含まれないため希望数も変わらない。

## 5. 編集・登録解除・再登録

- **講習コマのコマ表上での直接編集機能は設けない**（2026-06-09 オーナー決定）。
  - 科目・授業時間を変えたい場合は、対象コマを**「削除」し、生徒を新規追加して講習を手動追加**する（手動追加時に科目・特別講習・授業時間を選択）。
  - ※削除はストック由来なら希望数 −1（§4）。手動追加分は希望数に含まれない（が実配置＝講習回数(actual)にはカウントされる、§7）。
- 生徒日程表で登録解除すると、**その講習期間の該当生徒の講習コマを盤面から外す**。解除中は希望数が無効でストック基準にも入らない。
- 再登録すると、**解除前に配置で消費していた分も戻したうえで、希望数と一致するストックを再計算**。
- 講師日程表で登録解除すると、**その講師だけ盤面から外す**（生徒の講習予定は残る）。

## 6. 授業時間（★追加）

- 講習の**希望数提出時にも、通常授業と同じく授業時間 90／60／45分を選択できる**ようにする（提出経路＝⑦）。
- **コマ表での手動追加時にも授業時間 90／60／45 を選択できる**（2026-06-09 決定。コマ表編集の代替＝削除+手動追加の入口）。
- 未選択＝90分扱い。
- データ保持：提出由来は `SpecialSessionStudentInput.subjectDurations`（科目→分、未設定=90）。手動追加分は盤面コマの `noteSuffix`（60/45、空=90）に保持。

## 7. 表示・警告

- 盤面では**「通)」「講)」等の区分ラベル**で通常／講習を見分ける（色分けは撤廃済み・③）。
- 講師なし・制約違反・手動追加は**赤表示**、制約違反の理由はツールチップ。

---

## 現行からの主な差分（実装時TODO）

1. 盤面の「削除／空にする」で**講習希望数 −1**（従来：希望数は不変）。
2. 「削除」は**由来を問わず未配置へ戻さない**（従来：ストック由来は未配置へ戻していた）。
3. 講習コマの科目・時間を**コマ表上で直接編集可能**に（従来：登録解除→再登録のみ）。
4. 講習の希望数提出に**授業時間 90/60/45 の選択を追加**（→ 特別講習データ／提出ページ＝ブロック⑦、日程表＝⑨へ波及）。

## 実装状況（2026-06-09・Phase 5 着手）

- **TODO1 ✅ 実装**：生徒メニュー「削除」で `lessonType==='special' && specialStockSource==='session'` のとき希望数(`subjectSlots[subject]`)を −1。配置時の消費(-1)を打ち消す(+1)ことで残数会計を保持し、純減させる（未消化へは戻さない）。手動追加(`manual`)は希望数に含まれず変更なし。確認ダイアログ・ステータス文言も分岐。`ScheduleBoardScreen.tsx` `decrementSpecialSessionSubjectCount` / `handleDeleteStudent`。
- **TODO2 ✅ 既存充足**：削除は未配置へ戻さない（「戻す＝未消化講習に戻す」は別アクションとして既存）。
- **TODO3 ❌ 廃止**：講習コマのコマ表上編集機能は作らない（オーナー決定）。編集は「削除→生徒新規追加→講習を手動追加」で代替。
- **TODO4 一部✅**：
  - データモデル ✅：`subjectDurations?:Record<string,number>`（未設定=90）と `resolveLectureSubjectDuration` を追加（`specialSessionModel.ts`、後方互換＝破壊的移行なし）。
  - 手動追加の授業時間 ✅：手動追加UIに 90/60/45 を追加（`special` も `noteSuffix` を保持。`ScheduleBoardScreen.tsx` 追加メニュー＋確定処理）。盤面表示は既存の `displaySubjectWithNote` で自動反映。
  - 提出UIの授業時間（⑦）⏳ / 日程表への授業時間反映（⑨）⏳：後続ブロックで対応。
- **手動追加分の講習回数カウント ✅（既存で充足）**：日程表 `scheduleHtml.ts` の `lectureCounts`(actual) は配置済み `special` を由来問わず科目別に加算（手動も計上）。希望数(desired)は提出のみ＝手動を含めない。＝オーナー要望どおり。
- **「空にする」(日付一括 `handleClearStudentsOnDate`)** のセッション講習一括 −1 は TODO1 続きとして残（要バッチ希望数減算）。
