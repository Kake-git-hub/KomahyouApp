# 確定仕様：保存・バックアップ・復元（To-Be 正本）

> 全体見直しセッション（2026-06-07）でオーナー確認済みの「目標仕様」。
> 現行実装と差分があるため、本書がこの領域の正本。実装はこれに合わせて更新する。

## 1. 保存

- 自動保存は**残す**。デバウンス **5秒**／連続操作中は最大 **20秒** でまとめて保存。
  - （現行 2秒／15秒 から変更）
- 保存先は **Firebase（クラウド）一本**。クラウドが唯一の正本。
  - ~~ブラウザ内（IndexedDB/localStorage）への業務データ二段階保存は**廃止**。~~
    **【2026-06-10 訂正】二段階保存は維持**（廃止しない）。クラウドが唯一の正本である点は不変だが、
    ローカル保存はクラッシュ復旧用キャッシュとして残す（パフォーマンス主因ではないため）。詳細は末尾「オーナー決定」。
  - 表示範囲などの UI 設定 localStorage は対象外（残す）。
- 手動「保存」ボタンは**常時表示・常時クリック可能（グレーアウトしない）**。色は**青（`.primary-button`）で固定**。
  - **【2026-08-29 確定・オーナー確認済み／実装を正とする】** 旧仕様「常に緑」は**撤回**する。状態は**色ではなくラベル**で表す：
    未保存あり → **「保存」**（押すと保存）／保存済み → **「最新データ」**（押下は no-op）／保存中 → スピナー＋**「保存中…」**
    （二重実行防止のため、この間だけ disabled）。実装は `BoardToolbar.tsx` の `save-board-button`。
  - ボタンは `data-state`（`dirty` / `clean` / `saving`）を持つ。テスト・自動化・チェックリストは**この属性かラベルで**状態を判定する
    （色では判定しない）。
  - CSS の `.primary-button.is-clean`（緑）は旧仕様の名残で**どこからも付与されない死蔵クラス**。緑実装は行わないと決めたので、
    「緑にならない＝不具合」ではない（クラス自体の削除は別途の掃除タスク）。
- **閉じる／タブ切替で未保存があれば、離脱を止めて保存を促す。**
  - **【2026-07-04 確定・オーナー確認済み／C1：実装を正とする】** アプリ内の「手動保存を強制するダイアログ」は設けない。
    `beforeunload` ではブラウザ制約上アプリ独自のモーダルを表示できないため、**ブラウザ標準の離脱確認ダイアログ**
    （未保存・保存中・同期中のとき `event.preventDefault()`）で離脱を止め、あわせて**離脱時にローカル保存
    （同期 localStorage 書込）と Firebase 同期（`visibilitychange` hidden 時）を実行**することで代替する。
  - ~~日程表タブ以外~~ **タブ種別では分岐しない**。未保存があればタブに関係なく離脱確認を出す（実装が非依存で統一）。
- 自動保存（5秒）と離脱時の「離脱確認＋同期」は**両方残す**（二重の安全網）。

### 補足
- 自動保存の「重さ」は既に対策済み（盤面グリッド memo 化／週数の上限トリム／clone・stringify をデバウンス後へ移動）。
  自動保存自体は現状のパフォーマンス主因ではないため残す。

### 多端末競合の防止（楽観ロック）— 2026-07-04 確定・オーナー確認済み（監査 B1）

想定運用は「1端末でオンライン作業」だが、万一2端末目が同じ教室を編集した場合の**唯一の防波堤**として
楽観ロック（版数照合）を実装している。**過去のクロス汚染事故（他教室データでの上書き）への意図的な
破壊防止ガードであり、消してはならない。**

- クラウド保存は教室スナップショットの `version`（保存ごとに +1）で楽観ロックする。クライアントは
  「読み込んだ時点の版数（baseVersion）」を送り、サーバー版数と不一致なら**上書きせず拒否**する
  （`failed-precondition`。`functions/src/index.ts` `resolveOptimisticVersionDecision`）。
- 衝突時、このタブは (1) 内容を**JSON バックアップへ自動退避**、(2) 以後の**クラウド同期を停止**
  （古いベースでの再送ループを断つ）、(3) 「別の端末でこの教室のデータが更新されました。画面を再読み込みして
  ください」と**再読み込みを促す**。ローカル保存（IndexedDB）は継続するため編集内容はブラウザ内に残る。
- 版数は時刻比較では代替できない（多端末の時計ズレで誤判定するため必ず版数で照合する）。

### 1教室1タブ制約の範囲 — 2026-08-29 明文化・オーナー確定

「同じ教室を2つ開かせない」ロック（`useClassroomTabLock`）は **BroadcastChannel ＝ 同一ブラウザ（同一プロファイル）内の
タブ間でのみ**成立する。**別端末・別ブラウザ・シークレットウィンドウからの同時オープンは検知できない**。

- 検知できたときは後から開いたタブを**ブロック**する（開発者ロールは対象外。BroadcastChannel が無い環境では機能無効＝ブロックしない）。
- したがって**多端末の同時編集を防ぐ唯一の実効ガードは楽観ロック（版数照合・上記）**であり、タブロックは同一ブラウザでの
  うっかり二重オープンを減らす補助にすぎない。**タブロックがあるから多端末は安全、と読み替えてはいけない。**
- **UI で追加の警告は出さない**（2026-08-29 オーナー確定＝仕様として明文化するのみ）。運用は「1教室1端末」を前提とし、
  多端末衝突は楽観ロックの再読み込み案内で吸収する。

## 2. 保存失敗時

- **原則、自動リトライしない。**
  - **【2026-07-04 確定・オーナー確認済み／C2：例外を明文化】** ただし**一時的（transient）な同期エラーに限り、
    最大3回まで自動再試行**する（`manualFirebaseSaveStability`。ネットワーク瞬断・409 等の一過性失敗を吸収するため）。
    **恒久失敗（非 transient）および版数衝突（多端末上書き）は再試行せず**、下記の JSON 退避＋作業指示へ進む
    （版数衝突は同じ古いベースで再送しても必ず失敗するため、クラウド同期を停止して再読み込みを促す。§1「多端末競合の防止」参照）。
- JSON バックアップを**自動でダウンロードフォルダへ書き出す**。
- ダイアログで「再ログインして、ダウンロードフォルダのバックアップを読み込んでください」と作業指示を出す。

### 保存時の3層防御（破壊防止ガード）— 2026-07-04 確定・オーナー確認済み（監査 B2）

Cloud Function `saveClassroomSnapshot` は保存時に次の3層で本番データを守る。**いずれも過去のクロス汚染・
偽 dirty 上書き事故（他教室データや空データでの上書き）への意図的な破壊防止ガードであり、消してはならない。**

1. **空データ上書き拒否**（`assertNoSnapshotDataLoss`）：既存に教室管理データがあるのに、空（管理データ0件）の
   ペイロードで上書きしようとした保存は `failed-precondition` で拒否する（前回件数を添えて中止）。
2. **保存後の読み戻し検証**：保存直後に Firestore から読み戻し、`payloadHash` が一致しなければ `internal` で失敗させる
   （サイレント破損＝壊れたまま「保存済み」表示になる事故を防ぐ）。
3. **saveId による冪等リプレイ**：同一 `saveId`・同一内容の再送は再保存せず検証済み結果を返す。同一 `saveId` で
   内容が異なる再送は `already-exists` で拒否する（ネットワーク再試行の二重書き込み・取り違えを防ぐ）。

## 3. オフライン時

- **盤面を表示せず、アプリを使わせない。**
- 「オフラインのため使用できません」等の警告を出して操作をブロック。
- 想定運用：常に 1 人の室長アカウントが 1 端末でオンライン作業する。

## 4. 復元（4種類 → 1種類へ統合）

| 機能 | 扱い |
|------|------|
| 直前の Firebase 保存前へ戻す | **削除**（Undo で代替） |
| サーバーバックアップから復元 | 各教室画面から**削除**、**開発者画面のみ**へ移動 |
| ローカル自動バックアップから復元 | **削除** |
| JSON バックアップを読み込む | **残す（唯一の復元手段）** |

- 復元は「この教室だけ」に効き、他教室に影響しない（開発者画面のサーバー復元は教室選択モーダルで選んだ教室**だけ**を
  明示指定して保存し、他教室ドキュメントには一切書き込まない）。
- **復元時は確認モーダルで「元に戻せません」と警告してから実行。**
  - **【2026-07-04 確定・オーナー確認済み／C3】** この警告必須はそのまま維持する。監査時点では教室単位 JSON 読み込み・
    開発用教室への他教室読込には確認（上書き警告）があるが、**開発者画面のサーバーバックアップ復元モーダルには
    不可逆警告文言が無い**ため、実装側を本仕様に合わせて「復元すると選択教室の現在データは上書きされ、元に戻せません」を
    追加する（実装＋回帰テストは主セッションで対応）。

## 5. JSON バックアップ（書き出し／読み込み）

### 5-1. 教室単位（各教室画面）

- 書き出しは**開いている 1 教室分の“すべての状態・データ”**（テンプレート・設定・盤面・ストック等を含む）を 1 ファイルに。
- 読み込めば**完全復元**できること。読み込み時は確認（上書き警告）を出す。
- ファイル名は日本語＋日時入り。

### 5-2. 開発者画面：ワークスペース全体 — 2026-07-04 確定・オーナー確認済み（監査 B4）

- 教室単位（5-1）とは別に、**開発者画面のみ**「開発者バックアップ」として**ワークスペース全体
  （全教室・削除済み教室を含む）を1ファイルに書き出し／読み込み**できる。
- 読み込み時は**教室選択モーダル**で「復元する教室」を選ぶ（選んだ教室だけを復元し、選ばなかった教室は現状維持）。
  復元は選択教室のみを明示指定して保存し、他教室に影響しない（§4 と同じクロス汚染防止）。

### 5-3. 暗号化について — 2026-07-04 確定・オーナー確認済み（監査 B5）

- **JSON バックアップは平文（暗号化なし）**。読み込みにパスワードは不要。
- ローカル開発（Firebase 無効＝local モード）でのみ残っているパスワード入力プロンプトは**旧仕様の残骸**で、
  実際の復号処理は無い（将来撤去候補）。firebase モードでは渡さない（空文字）。

## 6. 初期設定フロー（現状維持）

- ①管理データ Excel 取込 ②机数・休校曜日 ③開始時点の未消化ストック（振替・講習）登録。
- 「初期設定を完了してコマ表をリセットする」で反映＋盤面再作成。
- 繰越（③の未消化振替）の在庫上の扱いは `spec-makeup-stock.md` §1-C／§5 が正本（初期設定日 `initialSetupCompletedAt` より前の休日は自動計上せず繰越で登録）。

## 7. その他

- 危険操作後の「直前の状態に戻す」バナー（Undo）… 維持。
- **開発用教室へのデータ取り込み — 2026-07-04 確定・オーナー確認済み（監査 B6）**
  - かつての「他教室の現状データを再現（開発用教室へ**コピー**）」は**廃止**。in-memory の他教室参照を使う浅いコピーが
    参照共有でクロス汚染事故を起こしたため（memory: classroom-restore-cross-contamination 第二の真因）。
  - 代替：**確定バックアップ（Storage）から「教室×時点」を選んで開発用教室へ読み込む方式（Feature B）**。
    サーバー callable でデータを取得し、**ディープクローン＋共有トークン除去**で参照を完全に断ってから
    **開発用教室（acting）だけ**へ書き込む（他教室には一切書き込まないため構造的に混入しない）。
  - 読み込み時は**取り違え防止の確認**を出す（読込元の規模＝生徒数／講師数／テンプレコマ数を提示し、想定と違えば中止を促す）。
  - 実行権限：**開発者アカウント、または開発用教室の室長**（開発用教室はサンドボックスのため任意教室を読み込める）。
    ~~室長へ一般開放~~ 一般教室の室長への開放は見送り（§末尾オーナー決定 2026-06-10）。

### QR 提出反映の永続化非対称と起動時の自己修復 — 2026-07-04 確定・オーナー確認済み（監査 B3）

QR 提出（講師/生徒）を盤面へ反映する機能には**永続化経路の非対称**があり、これを前提に自己修復を設けている。

- `countSubmitted`（提出反映フラグ。`specialSessions` 内）は**手動保存しなくても** workspace 自動同期で永続化される。
- 一方、QR 提出による**盤面への講師名配置（boardState の weeks/desks）は手動保存（Cloud Function snapshot）でしか
  永続化されない**。
- そのため手動保存前にリロードすると盤面配置だけ揮発し、`countSubmitted=true` は残るため取り込みでスキップされ
  「提出したのに出席可能コマに名前が出ない」状態でスタックしうる。
- **対処（維持）**：起動時（教室ロード/リロード毎に1回）に `reconcileSubmittedTeacherPlacements` が
  「`countSubmitted=true` なのに未配置の講師」だけを**冪等に**再配置する。既に一部でも配置済みの講師は触らない
  （手動調整・部分削除を尊重）。全コマ外したい場合は登録解除で対応する設計と整合。

## 8. データ保持期間（どのデータをどこまで遡れるか）— 2026-08-16 明文化・現行実装を正とする

「いつまで遡れるか」はデータ種別ごとに異なる。**本節がその正本**（各データの意味・業務ルールは
それぞれの正本ドキュメントに委譲し、ここでは**保持期間だけ**を定める）。

### 8-1. 一覧

| # | データ種別 | 保持期間 | 実装アンカー |
|---|-----------|---------|------------|
| 1 | **盤面の週（手動編集を含む週）**<br>出欠・振替・同日移動・講習・手動追加の生徒・メモ・手動指定講師 | **無期限**（使い始めた週まで全部） | `boardWeekTrim.ts` `weekHasManualBoardData` |
| 2 | 盤面の週（**通常授業のみ**＝手動編集ゼロ） | **過去 6 週／未来 26 週**の窓の外は破棄（テンプレから同一内容で再生成されるため実害なし） | `BOARD_WEEK_TRIM_PAST_WEEKS=6` / `BOARD_WEEK_TRIM_FUTURE_WEEKS=26` |
| 3 | 盤面の**週セル外**フィールド<br>`suppressedRegularLessonOccurrences` / `scheduleCountAdjustments` / `manualMakeupAdjustments` / `suppressedMakeupOrigins` / 手動ストック値 / `groupClassEntries` | **無期限**（週トリムの影響を受けない） | `PersistedBoardState`（`types/appState.ts`） |
| 4 | 管理データ（名簿・通常授業・特別講習・自動割振ルール・教室設定） | **現在値のみ／履歴なし**（過去時点の名簿は #5・#7 でしか辿れない） | 教室スナップショット |
| 5 | **講習の QR 提出**（`lectureSubmissions`） | **無期限**。サーバー側に自動削除は無く、消えるのは**登録解除**のみ。復元・ロールバックでも生き残るのが確定仕様 | `functions/src/index.ts`（`lectureSubmissions`） |
| 6 | 在籍生徒数の台帳（請求の正本・`studentCountLedger`） | **無期限**（write-once・毎月15日 0:10 JST 記録）。ただし**開始は 2026-08-15 の初回記録から。それ以前の月は遡れない** | `recordMonthlyStudentCounts`（cron `10 0 15 * *`） |
| 7 | **サーバー自動バックアップ**（Storage・15分ごと生成・非圧縮） | **日次（JST 3:00）は 400 日**、それ以外は下表 8-2 の段階間引き（2026-09-04 オーナー確定・7 日→400 日へ延長） | `shouldKeepWorkspaceAutoBackup`（`workspaceBackupSchedule.ts`） |
| 8 | **Google Drive ミラー**（**gzip 圧縮・`.json.gz`**） | 保持は **#7 と同一**（日次 400 日）。旧来の非圧縮 `.json` だけは **7 日**で間引く（移行期間の暫定・Drive 15GB 上限対策） | `shouldKeepGoogleDriveBackupByName` |
| 9 | 「直前に戻す」ロールバック（`workspace-latest-rollbacks/.../latest.json`） | **1 世代のみ**（保存のたび上書き） | `buildClassroomLatestRollbackStoragePath` |
| 10 | 復元前の安全スナップショット（incident backup） | **90 日**（2026-09-04 オーナー確定・従来は「間引き対象外＝残り続ける」だった）。作成時刻が読めないものは削除しない | `shouldKeepWorkspaceIncidentBackup` / `pruneWorkspaceIncidentBackups`（`cleanupOldSaveAttempts` が同じ巡回で掃除） |
| 11 | 保存の監査ログ（`saveAttempts`） | **30 日**（`SAVE_ATTEMPT_RETENTION_DAYS` で変更可・**最低 7 日**） | `cleanupOldSaveAttempts`（cron `30 3 * * *`） |
| 12 | **操作ログ**（`operationEvents`・在庫が減る／記録が消える操作） | **1 年**（`OPERATION_EVENT_RETENTION_DAYS` で変更可・**最低 30 日**） | 同上（`cleanupOldSaveAttempts` が同じ巡回で掃除） |
| 13 | **生徒授業台帳**（`lessonLedgerDays`・生徒×科目の授業実績と未消化・元コマ一覧つき・JST 日付ごと） | **2 年**（`LESSON_LEDGER_RETENTION_DAYS` で変更可・**最低 400 日**） | 同上（`cleanupOldSaveAttempts` が同じ巡回で掃除） |

補足：Hosting のリリース履歴（ロールバック A の戻し先）は `npm run hosting:retention:enforce`（既定 5 世代）で
制御する**手動運用**。デプロイ CI は自動実行しない。

### 8-1a. 復元前の安全スナップショット（incident backup）— 2026-09-04 オーナー確定で 90 日保持へ

`downloadServerAutoBackup`（＝復元の直前）で、全教室ぶんのワークスペーススナップショットを 1 本
`workspace-incident-backups/{workspaceKey}/` へ書き出している。復元をやり直すための最後の砦。

**従来は自動削除が無く**、本番に 3,978 本＝46.8GB が残置されていた（うち 3,943 本が 2026 年 5 月に集中。
検証で復元ダウンロードを反復すると 1 回ごとに数十 MB が積み上がるため）。当時の Cloud Storage 請求の
ほぼ全額がこの残置分だった。

- 保持は **90 日**（`WORKSPACE_INCIDENT_BACKUP_RETENTION_DAYS`）。自動バックアップ側が日次 400 日を
  保持しているので、こちらは「直近の復元操作をやり直せる」期間があれば足りるという判断。
- 作成時刻は GCS オブジェクトの `timeCreated` を正とする（ファイル名の解析には頼らない）。
  **作成時刻が読めないもの・未来日時のものは削除しない**（日付が読めないものを消すと事故になる）。
- 掃除は `cleanupOldSaveAttempts`（cron `30 3 * * *`）の同じ巡回で行う。**Firestore 側の巡回より先**に、
  **独立した時間予算**（90 秒）で走らせる。後ろに置いて予算を共有すると、Firestore 側が使い切った晩は
  1 ページだけ見て打ち切り、翌晩もまた 1 ページ目から読み直すため、滞留分が永久に減らない。
- 初回の削除対象は実測済み（2026-09-04 時点・読み取り専用で確認）: **3,963 本 46.31GB を削除、15 本 0.45GB を保持**。
- バケットは**ソフト削除 7 日**が有効。削除しても 7 日間は復旧でき、その間は容量課金も残る
  （「削除したのに請求が下がらない」と誤判定しないこと）。
- 消さずに対象をログ出力するだけの空実行は環境変数 `INCIDENT_BACKUP_PRUNE_DRY_RUN=1` で有効化する。

### 8-1b. 操作ログ（`operationEvents`）— 2026-09-04 新設・オーナー確定

「未消化振替が減った／消えた」という問い合わせに答えるための**監査記録**。従来は 15 分毎の自動バックアップ（#7）を
前後比較して**推測**するしかなく、「いつ誰が × で消したか」は残っていなかった（実際に 2026-09-04 の緑が丘校の調査で
この限界に当たった）。

- **人の操作で記録するのは在庫が減る／記録が消える操作だけ**（オーナー確定）。配置・移動などの日常操作は記録しない
  （件数を抑え、問い合わせに必要な情報だけ残す）。種別の正本は `functions/src/operationEvents.ts` の
  `OPERATION_EVENT_KINDS`：未消化振替の × 削除／未消化講習の × 削除／コマ削除／格納／出欠付与（上書きで消える
  前の生徒の記録も detail に残す）／出欠解除／その日の生徒を全コマ削除／丸ごと振替／休日設定・解除／
  生徒移動の「移)」マーカーによる他人の出欠記録の上書き（`record-displaced`）。
- **自動処理は例外として「配置」も記録する**（2026-09-04 オーナー指示 ★2）。人の操作ではないため上の原則の外で、
  「触っていないのに講師が消えた／湧いた」の調査に必要: 起動時の自己修復 `auto-teacher-reconcile`／QR 提出・登録解除に
  伴う講師の自動登録・解除 `auto-teacher-assign`／生徒の登録解除に伴う講習コマ除去 `auto-student-unassign`。
  ※テンプレ再マージ・机の詰め直し・生徒側の自動割振は未記録（必要になったら追加する。ログに無い＝起きていない、ではない）。
- **置き場所は教室スナップショットの外**（`classroomSnapshots/{classroomId}/operationEvents/{eventId}`）。
  ★盤面データに混ぜてはいけない：復元・ロールバックで「消した記録」ごと巻き戻り、監査に使えなくなる。
- **実行者（`updatedBy`）と受領時刻（`recordedAt`）は Cloud Function が付ける**。クライアントの自己申告にしない。
- クライアントは操作のたびにバッファへ積み、**次の保存に相乗り**させて送る（`src/utils/operationLog.ts`）。
  保存が失敗したらバッファへ戻して再送する。**イベント id をドキュメント id にする**ので再送しても重複しない。
- ★**保存が確定してから書く**。保存に失敗した操作は盤面にも残らないため、記録も残さない。
  逆に**ログの書き込みに失敗しても保存は成功のまま**にする（監査記録は保存より優先度が低い）。
- 画面表示は無し（2026-09-04 時点）。オーナー確定で「まず記録だけ」。閲覧は開発側が Firestore を読む。
  表示を足すときは、この保存経路（相乗り・サーバー採番）を変えずに読み取りだけ追加する。

### 8-1c. 生徒授業台帳（`lessonLedgerDays`）— 2026-09-04 新設・オーナー指示

操作ログ（8-1b）は「減った・消えた理由」を残すが、**「ある時点で各生徒の授業数・未消化数がいくつだったか」は
復元できない**（残数は保存せず毎回計算し、過去の盤面は #7 のバックアップに頼る。当時は 7 日分しか無かった）。**未消化数は重要な項目**なので、
任意の時点の状態をコマ単位で振り返れるよう、生徒×科目ごとの台帳を日付ごとに残す。

- **内容**（`src/utils/studentLessonLedger.ts`）: 生徒×科目ごとに、未消化振替の残数と**元コマ一覧**
  （`YYYY-MM-DD#限|理由`）、出席／休み／振無休／未出欠の配置のコマ一覧（`YYYY-MM-DD#限|種別|振替元日`）、
  未消化講習の残数と元コマ一覧、合計値。
- ★**計算は盤面画面と同じ関数**（`buildMakeupStockEntries` / `buildLecturePendingItemsByEntryKey`）と同じ生徒キー規則で行う。
  画面の未消化数と台帳の数字が食い違わないことが最優先。**盤面側の集計を変えたら台帳も同じ結果になることを確認する。**
- **記録のタイミング**: 保存のたびにクライアントが計算し、**内容が変わったとき（または JST の日付が変わったとき）だけ**
  保存に相乗りさせる。サーバーは **JST 日付ごとに 1 ドキュメント**へ書き、同じ日の保存は上書き（＝その日の最終状態）。
  保存が無い日はドキュメントが無いので、「その日の状態」は**その日以前で最新の台帳**を読む。
- **置き場所**: `classroomSnapshots/{classroomId}/lessonLedgerDays/{YYYY-MM-DD}`。本文は gzip+base64、
  合計値・行数は平文（一覧だけで推移を追える）。スナップショット本体には入れない（8-1b と同じ理由）。
- 実績・配置のコマ一覧は**直近 400 日分**に絞る（盤面は週を無期限保持するため、何年も経つと肥大化する。
  未消化の元コマ一覧はこの制限を受けない）。容量の目安は生徒 100 名規模で圧縮後 1 日 50〜100KB。
- 保存本体より優先度が低い: 計算・書き込みに失敗しても保存は成功のまま（操作ログと同じ方針）。
- 閲覧は `node tools/lesson-ledger-report.mjs <classroomId> [--date YYYY-MM-DD] [--student 氏名]`（読み取り専用・gcloud ログイン済み PC）。
  画面表示は無し（2026-09-04 時点）。

### 8-2. 自動バックアップの段階間引き（#7・#8 共通）

生成は **15 分ごとの 1 本**に一本化し、保持は**保存時刻の経過時間**だけで判定する（生成種別に依存しない）。

| 経過時間 | 残す粒度 |
|---------|---------|
| 0〜24 時間 | 15 分刻み（全保持） |
| 24〜72 時間 | JST 毎時 0 分のみ（実質毎時） |
| 72 時間〜400 日 | JST 3:00 のみ（実質日次） |
| 400 日超 | 削除 |

- 定常状態の保持本数は概算 96＋48＋397 ≒ **540 本**（2026-09-04 に日次を 7 日→400 日へ延長。Storage は非圧縮
  1 本約 57MB で約 23GB・月 70 円程度。Google Drive は gzip で約 1/8 なので約 3GB）。
- **Google Drive の復元**: ミラーは `.json.gz`。アプリの「バックアップを読み込む」は gzip のマジックバイトで判定して
  ブラウザ内で解凍する（`src/utils/backupFileText.ts`）ので、解凍せずそのまま取り込める。gzip は可逆で、
  解凍結果は Storage の非圧縮 JSON とバイト単位で同一（往復テストで固定）。
- **静音時間帯（JST 3:15〜8:45）は生成を休止**して取得回数を抑える。ただし**日次アンカーの 3:00 は必ず取得**する
  （取らないと 72 時間〜400 日帯が丸ごと存在しなくなる）。
- Google Drive 側の削除は Drive API の `DELETE /files/{id}`＝**ゴミ箱を経由しない完全削除**。「Drive のゴミ箱から拾う」はできない。
- Drive の間引き対象は「**設定フォルダ内・`trashed=false`・`appProperties.workspaceKey` 付き**」のファイルのみ。
  手動で別フォルダへコピーしたものは間引かれず残る（長期保管したい場合の逃げ道）。

### 8-3. この保持設計の帰結（運用上の約束）

- **「その日の教室データを丸ごと当時の状態に戻す」は、日次（JST 3:00 時点）なら 400 日、15 分刻みなら 24 時間、毎時なら 72 時間が限界。**
  400 日は 2026-09-04 の延長以降に積み上がる分で、それ以前へは遡れない。それより前の誤操作はバックアップ復元では取り返せない。
  Google Drive 同期は**冗長化**（Web アプリを開かなくても JSON が残る／Firebase 障害時に手元にある）が目的であり、
  **長期アーカイブではない**。
- 一方で **「いつ誰が出席したか」（出欠）と講習の提出実績は無期限で残る**（#1・#5）。日々の実績照会に
  バックアップ復元は不要。
- **過去の在籍数（退塾済みを含む当時の人数）は 2026-08-15 以降のみ確定値**（#6）。それ以前は現名簿からのライブ計算＝
  当時と一致しない可能性がある。台帳が導入されたのはこの穴を塞ぐため。

### 8-4. 変更時の手順（厳守）

保持期間は**ユーザーへの約束**であり、コードの都合で黙って変えてよい数値ではない。

- 期間を変えるときは **(1) 本節の更新 (2) 定数のスペックロックテストの更新 (3) `CHANGELOG.md` への記載**を
  **同じコミット**で行う。テストは `functions/src/workspaceBackupSchedule.test.ts` および
  `src/components/schedule-board/boardWeekTrim.test.ts` の「保持期間のスペックロック」節。
- 特に **#1（手動編集を含む週は無期限）を弱めない**。`weekHasManualBoardData` の判定を1つでも落とすと、
  出欠・振替・メモを持つ過去週が静かに消える（データ消失＝最悪の回帰）。
- **#7 と #8 は同じ段階間引きを共有している。** 2026-09-04 に**意図的な非対称**を 1 つ入れた: Drive は gzip
  （`.json.gz`）でミラーし、旧来の非圧縮 `.json` だけは 7 日で間引く（`shouldKeepGoogleDriveBackupByName`・Drive 15GB 上限対策）。
  これ以外の「Drive だけ長く残す」等の非対称化は、意図的な分岐として
  本節に明記してから実装する。

---

## 現行実装からの主な差分（実装時のTODO覚書）

1. デバウンス 2s/15s → **5s/20s**。
2. ブラウザ内二段階保存の**廃止**（クラウド一本化）。
3. 保存ボタン：disabled/グレーアウト廃止 → **常時緑**。
4. 離脱時（未保存）**ダイアログで手動保存強制**（日程表タブ除く）。
5. 保存失敗：リトライ廃止 → **JSON自動DL＋再ログイン指示ダイアログ**。
6. **オフライン時は全面ブロック**＋警告。
7. 復元 3 種（rollback／サーバー／ローカル自動）を教室画面から**削除**。サーバー復元は開発者画面へ。
8. JSON 書き出しを**教室フルスナップショット**化（テンプレ含む完全復元）。
9. 開発用教室コピーを**室長アカウントにも開放**。

## 現状精査（2026-06-10・Phase 6 ② 着手時）

| # | 項目 | 状態 | 根拠 |
|---|------|------|------|
| 1 | デバウンス 5s/20s | ✅ 済 | `App.tsx` `AUTOSAVE_DEBOUNCE_MS=5000` / `AUTOSAVE_MAX_WAIT_MS=20000` |
| 2 | 二段階保存廃止 | ❌ 未 | `saveWorkspaceSnapshot`(IndexedDB+localStorage)が自動保存・離脱flush・手動保存・教室切替等 約8箇所で稼働中。pending-remoteマーカー機構（クラッシュ復旧・再ログイン時のローカル→リモート同期）がローカル書込に依存 |
| 3 | 保存ボタン常時緑 | ✅ 済 | `BoardToolbar` 常時表示・「保存/最新データ」表示 |
| 4 | 離脱時ダイアログ | ✅ 済(同等) | `beforeunload` で未保存時にブラウザ標準の離脱確認＋`visibilitychange` でFirebase自動同期。「手動保存の強制」ではないが二重安全網は充足 |
| 5 | 保存失敗 JSON自動DL | ✅ 済 | 失敗時バックアップ自動DL＋再ログイン手順ダイアログ |
| 6 | オフラインブロック | ✅ 済 | `OfflineGate.tsx` |
| 7 | 復元3種の削除/移動 | ✅ **実装（2026-06-10）** | 下記「実装状況」 |
| 8 | JSONフルスナップショット | ✅ 済 | `exportBackup`=AppSnapshot完全形式（Phase 0） |
| 9 | 開発用コピー室長開放 | △ UIのみ | パネルは `isDevelopmentClassroom` で室長にも表示されるが、コピー元一覧が `workspaceClassrooms`（メモリ）依存。**室長は自教室しかロードされないためコピー元が常に空＝実体未開放**。真の開放はサーバー側コピー（Cloud Function 例: `copyClassroomDataToDevelopmentClassroom` のサーバー版）が必要（室長への他教室読取権限付与は不可） |

## 実装状況（2026-06-10・Phase 6 ②）

- **TODO7（復元UI整理）✅ 実装（未デプロイ）**：
  - `BackupRestoreScreen.tsx`：教室画面から「直前のFirebase保存前へ戻す」「サーバーバックアップから復元」「ローカル自動バックアップから復元」の3パネル＋2モーダルを削除。復元は**JSONバックアップ読み込み一本**。開発用教室コピー・初期設定フローは維持。
  - `App.tsx`：`restoreClassroomFromServerAutoBackup`／`restoreLatestClassroomRollback`／`restoreLocalAutoBackup`／`refreshLocalAutoBackupSummaries`／`localAutoBackupSummaries` を削除（教室画面専用だった）。開発者画面のサーバー復元（`onRestoreServerAutoBackup`→教室選択モーダル）は既設のまま＝spec「サーバー復元は開発者画面へ」充足。
  - テンプレート上書き前のローカル退避（`savePreTemplateSaveBackup` の書き込み側）は安全網として維持。

## オーナー決定（2026-06-10）— ②は完了

- **TODO2（二段階保存廃止）＝見送り・現状維持に正本訂正**。ローカル保存（IndexedDB/localStorage）の二段階保存は**廃止しない**。
  - 理由：自動保存の重さは既に対策済み（盤面 memo 化／週トリム／clone・stringify をデバウンス後へ移動）で、**二段階保存のローカル書込自体は現状のパフォーマンス主因ではない**（オーナー判断）。
  - 位置づけの訂正：クラウド（Firebase）が**唯一の正本**である点は不変。ローカル保存は**クラッシュ復旧用キャッシュ**（pending-remoteマーカーによる再ログイン時のローカル→リモート復旧）として維持する。§1 の「二段階保存は廃止」は本決定で撤回。
- **TODO9（開発用コピーの室長開放）＝見送り**。Cloud Function 新設コスト／室長への他教室読取権限付与の是非を踏まえ、**開発者アカウントのみ**で維持（現状のまま）。§7 の「室長へ拡張」は本決定で撤回。
- **TODO7（復元UI整理）＝実装・デプロイ**。

→ **② クラウド一本化＝完了**（TODO7 を反映、TODO2/9 はオーナー決定で現状維持/見送り。他項目は既存充足）。これで **Phase 6 完了**。
